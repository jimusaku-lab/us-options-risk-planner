# Saxo P/N口座・N口座ホイール対応 設計書 v2

作成日: 2026-05-31  
対象: 米国株オプション建玉管理・リスク確認アプリ  
初回実装対象: ローカルPC版 `http://127.0.0.1:5173/`  
関連資料:

- `saxo_p_n_account_logic_report.md`
- `No7_サクソバンクのサブ口座と株式移管.pdf` 1-12ページ
- `saxo-readonly-upgrade-design-handoff.md`
- `tradingview-saxo-integration-brief.md`

## 1. 改訂の目的

これまでの手入力型パイロットは、JPYを主軸にした建玉管理として成立している。

ただし、サクソバンク証券で実際に運用する前提では、次の設計が不足していた。

- P口座とN口座の決済通貨差
- N口座を主戦場にしたUSDベースのホイール戦略
- P口座で取得した株式をN口座へ株式移管する流れ
- N口座内で、カバードコール、株式売却、プット売り、株式取得を循環させる管理
- ショートストラングル等で発生する、現物株を保有しないコール売りの上方向リスクと撤退ルール
- 最終出金時だけ円転するという為替設計

本設計書は、これらをアプリの正式なバックグラウンドロジックとして組み込み、以後の実装工程を明確にする。

## 2. 最終目標

最終目標は、サクソバンク証券のP/N口座構造に合わせて、米国株オプションの建玉、株式取得、株式移管、カバードコール、プット売り、税務区分、為替換算を一貫して管理できるアプリにすること。

特にホイール戦略は、原則として **N口座内でUSDベースで回す**。

```text
N口座でUSD保有
→ N口座でプット売り
→ 権利行使でN口座に株式取得
→ N口座でカバードコール
→ 権利行使で株式売却
→ N口座にUSDが残る
→ N口座で再購入または再度プット売り
```

P口座は、円資金から入口を作る補助ルートとして扱う。

```text
P口座でプット売り
→ 権利行使でP口座に株式取得
→ P口座からN口座へ株式移管
→ 以後はN口座ホイールへ合流
```

このアプリは、今後Saxo read-only連携を入れる場合でも、発注はしない。最終判断と発注はユーザーがSaxo画面で行う。

## 3. 重要な設計原則

### 3.0 DEMO口座は本番N口座相当として扱わない

SaxoTraderGOのDEMO口座は、米国株式・米国株式オプションの価格や建玉損益がUSDで表示される一方、画面下部の口座通貨、現金残高、買付可能額、評価額はJPYで表示される。

そのため、アプリ上では本番N口座相当として扱わず、次の3分類で扱う。

```ts
type AccountEnvironment =
  | "DEMO_JPY_BASE"
  | "PROD_P_JPY_SETTLEMENT"
  | "PROD_N_USD_SETTLEMENT";
```

- `DEMO_JPY_BASE`: デモ・テスト用。USD建て商品をJPY口座で管理している状態
- `PROD_P_JPY_SETTLEMENT`: 本番P口座。JPY決済
- `PROD_N_USD_SETTLEMENT`: 本番N口座。USD決済

DEMOワークスペースでは、口座全体カードにN口座欄を表示しない。

DEMO表示:

```text
DEMO / JPYベース
証拠金使用率
現金残高
必要証拠金など、Saxoデモ画面下部でJPY表示される項目
```

DEMOで非表示にするもの:

- N口座
- USD残高
- N口座証拠金使用率
- N口座ホイール用USD台帳

REALワークスペースの場合だけ、P口座とN口座を分けて表示する。

既存データの移行:

- 既存DEMOワークスペース: `DEMO_JPY_BASE`
- 既存REALワークスペースで口座種別がないもの: 原則 `PROD_P_JPY_SETTLEMENT`
- REAL側は必要に応じてユーザーがN口座へ変更できるようにする

注意:

- `DEMO_JPY_BASE` は本番P口座と完全に同じではない
- 画面上は「P口座」ではなく「DEMO / JPYベース」と表記する
- DEMO口座はP口座ロジックの初期テストには使える
- DEMO口座をN口座ロジック、USD残高管理、JPY→USD資金振替、USD取得レート管理の検証には使わない

### 3.1 P/N口座を合算しない

P口座とN口座は、同じサクソバンク証券内の外国株式等オプション口座であっても、アプリ上は別口座として扱う。

```ts
type SaxoAccountCode = "P" | "N";
type Currency = "JPY" | "USD";
```

- P口座: JPY決済
- N口座: USD決済
- 証拠金、現金残高、株式保有、オプション建玉は口座別に管理する
- P口座のJPY残高をN口座の余力として扱わない
- N口座のUSD残高をP口座の余力として扱わない

### 3.2 N口座の損益はUSDが主

N口座では、プレミアム、手数料、株式売買代金、実現損益、累積ホイール損益をUSDで管理する。

JPY表示は参考換算に限定する。

```text
N口座のJPY換算額は、表示用USD/JPYによる参考値です。
円転または税務上の確定損益とは異なる可能性があります。
```

### 3.3 為替は3種類に分ける

1. P口座の取引時適用為替
2. N口座の参考表示用USD/JPY
3. USDをJPYに戻す出金・円転時の為替

N口座内でホイールを回している間、USD建て損益は為替更新で変えない。

### 3.4 P口座の円転コストは二重計上しない

P口座でブローカー実明細のJPY決済額、JPY手数料、適用為替が分かる場合、それを優先する。

アプリ側で一律に0.25%を上乗せしない。

0.25%は、JPY⇔USDの資金振替イベントの推定時だけ既定値として使う。

### 3.5 税務区分は分ける

アプリ上は次を別区分として表示する。

- オプション損益: 先物取引に係る雑所得等
- 株式売却損益: 上場株式等の譲渡所得等
- 為替差損益: 今回は厳密計算の対象外。出金・円転イベントとして記録し、参考表示から始める

本アプリの税額は概算であり、申告用の確定値ではない。

## 4. 口座と台帳のデータ設計

### 4.1 口座マスタ

```ts
type BrokerAccount = {
  id: string;
  broker: "SAXO_BANK_JP";
  accountCode: "P" | "N";
  displayName: string;
  baseCurrency: "JPY" | "USD";
  settlementCurrency: "JPY" | "USD";
  productType: "FOREIGN_STOCK_INDEX_OPTIONS";
};
```

表示例:

- `P: 外国株式等オプション（円建て / JPY決済）`
- `N: 外国株式等オプション（ドル建て / USD決済）`

### 4.2 口座状態

現在の `AccountInputs` はJPY前提なので、P/N別に拡張する。

```ts
type AccountState = {
  accountCode: "P" | "N";
  currency: "JPY" | "USD";
  cashBalance: number;
  marginAvailable: number;
  buyingPower?: number;
  marginUsagePercent: number;
  accountValue?: number;
  updatedAt: string;
  cashAdjustments?: AccountCashAdjustment[];
};

type AccountCashAdjustment = {
  id: string;
  sourceType: "option_close_execution";
  sourceSimulationId: string;
  sourceExecutionId: string;
  accountCode: "P" | "N";
  currency: "JPY" | "USD";
  amount: number;
  label: string;
  appliedAt: string;
  memo?: string;
};
```

P口座:

- 現金残高 JPY
- 買付可能額 JPY
- 口座純資産 JPY
- 必要証拠金余力 JPY
- 証拠金使用率

N口座:

- USD現金残高
- USD買付可能額
- USD口座純資産
- 必要証拠金余力 USD
- 証拠金使用率
- 参考JPY換算

SaxoTraderGOの画面下部に表示される項目は `必要証拠金` ではなく `必要証拠金余力` である。アプリの口座全体カードでは、利用者がSaxo画面からそのまま転記できるよう、入力ラベルを `必要証拠金余力` に統一する。

設計上の意味:

```text
必要証拠金余力 = 追加で証拠金として使える余力
必要証拠金 = 既存建玉で拘束・要求されている証拠金
```

本アプリでは、Saxo画面下部に表示される `必要証拠金余力` を入力対象にする。`必要証拠金` そのものをユーザーに探させない。

計算上の扱い:

- `証拠金使用率` はSaxo表示値をそのまま入力し、警告判定に使う
- `必要証拠金余力` は、口座全体の余力確認と参考表示に使う
- P売り権利行使時の買受資金チェックは、原則として `現金残高` または `買付可能額` と比較する
- `必要証拠金余力` を、株の買受資金そのものとして扱わない
- 分母計算に使う個別建玉の証拠金は、従来どおり建玉入力側の `チケット表示証拠金` と `証拠金バッファ` から計算する

既存データ互換:

- 旧 `marginRequirement` は読み込み時に `marginAvailable` へ移行する
- 旧JSONの値を失わない
- UI上は `必要証拠金余力` と表示し、`必要証拠金` とは表示しない

### 4.3 オプション建玉

既存の `TradeSimulation` に `accountCode` と通貨別キャッシュフローを追加する。

```ts
type TradeSimulation = {
  accountCode: "P" | "N";
  accountCurrency: "JPY" | "USD";
  referenceFxRateJPY?: number;
  brokerSettlement?: BrokerSettlement;
  // 既存項目...
};
```

```ts
type BrokerSettlement = {
  source: "manual" | "broker_statement" | "saxo_api_estimate";
  tradeCurrency: "USD";
  settlementCurrency: "JPY" | "USD";
  grossPremiumUSD: number;
  commissionUSD?: number;
  commissionJPY?: number;
  exchangeFeeUSD?: number;
  exchangeFeeJPY?: number;
  appliedFxRate?: number;
  netCashflowUSD?: number;
  netCashflowJPY?: number;
};
```

## 5. 為替・資金振替・出金設計

### 5.1 FX_TRANSFER

JPY⇔USDの資金振替は、オプション取引とは別イベントとして扱う。

```ts
type FxTransferEvent = {
  id: string;
  direction: "JPY_TO_USD" | "USD_TO_JPY";
  fromAccountCode: "P" | "N" | "OTHER_JPY" | "OTHER_USD";
  toAccountCode: "P" | "N" | "OTHER_JPY" | "OTHER_USD";
  fromCurrency: "JPY" | "USD";
  toCurrency: "JPY" | "USD";
  fromAmount: number;
  toAmount: number;
  appliedFxRate: number;
  fxCostRate?: number;
  occurredAt: string;
  source: "manual" | "broker_statement" | "app_estimate";
};
```

推定時の既定値:

```ts
const DEFAULT_SAXO_TRANSFER_FX_COST_RATE = 0.0025;
```

ただし、実明細の `fromAmount`, `toAmount`, `appliedFxRate` がある場合はそれを優先する。

### 5.2 N口座からの最終出金

N口座ホイールで得たUSD利益は、N口座内ではUSD利益として管理する。

最終的に出金したい場合のみ、出金・円転イベントを記録する。

```text
N口座USD残高
→ USD_TO_JPY出金・円転
→ その時点の為替でJPY換算
```

このイベントは、N口座内ホイールの運用成績とは別に表示する。

## 6. 株式移管設計

### 6.1 P→N株式移管イベント

P口座でプット売りが権利行使され株式を取得した場合、その株式をN口座へ移管する運用を想定する。

```ts
type StockTransferEvent = {
  id: string;
  ticker: string;
  fromAccountCode: "P" | "N";
  toAccountCode: "P" | "N";
  shares: number;
  transferDate: string;
  costBasisUSD: number;
  sourceSimulationId?: string;
  destinationWheelCycleId?: string;
  memo?: string;
};
```

株式移管は売却ではないため、原則として以下の扱いにする。

- オプション損益に含めない
- 株式譲渡損益に含めない
- 為替差損益に含めない
- 株数と取得単価をN口座側へ引き継ぐ
- N口座ホイールの `stock_holding` フェーズへ合流させる

### 6.2 P口座で株式が残る場合の警告

P口座で株式取得済みだがN口座へ移管していない場合、次の注意を表示する。

```text
P口座で取得した株式が残っています。
N口座へ株式移管してカバードコール管理へ進めるか確認してください。
```

## 7. N口座ホイール戦略設計

### 7.1 ホイールの基本単位

ホイールは銘柄ごとに管理する。

```text
1銘柄 = 1ホイールサイクル
```

NVDA、AMZN、NFLXなど、銘柄ごとにカードを表示する。

### 7.2 ホイールフェーズ

```ts
type WheelPhase =
  | "n_cash"
  | "n_short_put"
  | "n_stock_holding"
  | "n_covered_call"
  | "n_called_away"
  | "p_short_put"
  | "p_assigned_stock"
  | "p_to_n_transfer_pending"
  | "cycle_closed";
```

標準ルート:

```text
N現金待機
→ Nプット売り中
→ N株式保有中
→ Nカバードコール中
→ N株式売却済み
→ N現金待機
```

P口座合流ルート:

```text
Pプット売り中
→ P株式取得済み
→ P→N株式移管待ち
→ N株式保有中
→ Nカバードコール中
```

P口座合流ルートは、N口座ホイール本体ではなく「N口座ホイールへ合流する前段階」として扱う。P口座でプット売りをしているだけの状態では、N口座の現金待機を「済」と表示しない。N口座にUSD現金がない、またはN口座で実際に建玉・株式保有がない場合は、N口座側のフェーズを進行済みに見せない。

表示上は次のように分ける。

```text
P口座準備ルート:
Pプット売り中
→ P株式取得済み
→ P→N移管待ち

N口座ホイール本体:
N現金待機
→ Nプット売り中
→ N株式保有中
→ Nカバードコール中
→ N株式売却済み
```

P口座準備ルートのカード名は `N口座ホイール` と断定せず、`ホイール準備中（P口座）` または `P口座からN口座への合流準備` とする。N口座へ移管済み、またはN口座で建玉を開始した段階で `N口座ホイール` と表示する。

### 7.3 ホイールサイクル

```ts
type WheelCycle = {
  id: string;
  ticker: string;
  primaryAccountCode: "N";
  currentPhase: WheelPhase;
  currentAccountCode: "P" | "N";
  currentShares: number;
  averageCostUSD: number;
  usdCashImpact: number;
  cumulativePremiumUSD: number;
  cumulativeStockRealizedPnlUSD: number;
  cumulativeFeesUSD: number;
  cumulativeTotalPnlUSD: number;
  referenceFxRateJPY?: number;
  eventIds: string[];
  linkedSimulationIds: string[];
  openedAt: string;
  closedAt?: string;
  memo?: string;
};
```

`primaryAccountCode: "N"` は「最終的にN口座でホイールを回す設計上の主口座」を表す。現在の実建玉口座ではない。画面上で `主口座 N / 現在 N` のように表示すると誤解しやすいため、次のように表示する。

- P口座準備中: `現在: P口座 / N口座合流前`
- P→N移管待ち: `現在: P口座 / N口座へ移管待ち`
- N口座で運用中: `現在: N口座ホイール`

`主口座 N` という内部概念は、利用者向けの常時表示から外すか、ヘルプ内だけで説明する。

### 7.4 ホイールイベント

```ts
type WheelEvent =
  | ShortPutOpenedEvent
  | ShortPutClosedEvent
  | PutAssignedEvent
  | StockTransferEvent
  | StockPurchaseEvent
  | CoveredCallOpenedEvent
  | CoveredCallClosedEvent
  | CallAssignedEvent
  | StockSoldEvent
  | FxTransferEvent
  | ManualAdjustmentEvent;
```

イベント履歴は時系列で表示する。

```text
日付 | イベント | 口座 | 内容 | USD損益 | 株数変化 | フェーズ
```

### 7.5 ホイール可視化

画面下部のホイール戦略は、表だけではなく、色付きステップで現在地を示す。

```text
N現金待機 ─ Nプット売り ─ N株式保有 ─ Nカバードコール ─ N株式売却
  済           済           済           現在             未
```

色:

- 灰色: 未到達または該当なし
- 緑: 通過済み
- 青: 現在
- 黄: 注意
- 赤: 対応が必要

ステップの「済」は、その口座で実際に通過した状態だけに使う。P口座でプット売り中の場合、N口座現金待機を通過済みとして緑表示しない。N口座の現金が0 USDで、N口座側の建玉・株式保有もない場合は、N口座ステップは未到達として表示する。

P口座準備中の例:

```text
NVDA  ホイール準備中（P口座）
現在: P口座でプット売り中 / N口座合流前

Pプット売り ─ P株式取得 ─ P→N移管待ち ─ N株式保有 ─ Nカバードコール
  現在           未             未             未             未
```

この状態では、N口座の累積プレミアム、N口座現金、N口座ホイール損益を0のまま「済」扱いにしない。

カード例:

```text
NVDA  N口座ホイール
現在: Nカバードコール中
累積プレミアム +$428
累積損益 +$612
保有株数 100株 / 平均取得 $200.00

N現金待機 ─ Nプット売り ─ N株式保有 ─ Nカバードコール ─ N株式売却
  済           済           済           現在             未

次の候補: Cを満期まで保有するか、買戻しで閉じるかを確認
```

### 7.6 建玉管理との役割分担

建玉ダッシュボード:

- いま存在する1件ごとの建玉
- 注文前、建玉中、決済済み、権利行使済み、満期終了

ホイール戦略:

- 同じ銘柄の一連の履歴
- 累積プレミアム
- 累積株式売買損益
- 現在フェーズ
- 次に取る候補アクション

## 8. 画面設計

### 8.1 ヘッダー

追加または明確化する表示:

- DEMO / REAL
- P口座 / N口座フィルタ
- 株価更新
- 為替更新
- JSONバックアップ / 復元

### 8.1.1 株価・為替の外部取得方針

株価・為替の取得は、利用者が通常行う市場情報確認と同じ位置づけとして扱う。ヘッダーに `外部取得ON/OFF` の大きな切替ボタンは置かず、株価更新・為替更新ボタンは常に使用可能にする。

既存データやlocalStorageに `外部取得OFF` 相当の保存値が残っている場合でも、今後はその値で株価・為替ボタンを無効化しない。旧フラグは読み込み時に無視するか、内部的にON相当へ移行する。

目的:

- ヘッダーの操作を建玉管理に必要なボタンへ絞り、画面を圧迫しない
- 株価更新・為替更新を、常にすぐ使える基本機能にする
- 銘柄ティッカーとUSD/JPYだけを外部サービスへ照会できる
- 建玉数量、口座残高、証拠金、JSONバックアップなどの入力データは外部送信しないことを明示する
- 取得に失敗しても、手入力で続行できる状態を維持する

説明文の配置:

`使い方` または `データ` 画面に、次の説明を置く。ヘッダーには常時表示しない。

```text
株価・為替の更新ボタンを押すと、価格取得に必要な最小限の情報だけを外部の価格取得サービスへ送信します。
送信される可能性があるもの:
- 銘柄ティッカー（例: NVDA, AMZN）
- USD/JPYの取得リクエスト
- 利用者のIPアドレス、ブラウザ情報、アクセス時刻

送信しないもの:
- 保有株数
- 建玉数量
- プレミアム
- 口座残高
- 証拠金使用率
- DEMO/REALの入力内容全体
- JSONバックアップの内容
- localStorageに保存されたデータ

取得できない場合は、Saxo TraderGO等で確認した株価・為替を手入力してください。
```

UI:

- ヘッダーから `外部取得ON/OFF` ボタンを削除する
- `株価` ボタンと `為替` ボタンは常に有効にする
- 旧OFF状態による赤い通知文 `外部取得をOFFにしました。株価・為替は手入力してください。` は表示しない
- 取得失敗時だけ、どちらが失敗したかを短く表示する
- 取得失敗後も、入力欄は手入力可能なままにする
- 自動取得の説明は `使い方` または `データ` 画面に置き、必要な利用者だけ確認できるようにする

実装上の注意:

- 外部取得では、送信するのは価格取得に必要な最小情報に限定する
- URLに建玉ID、数量、口座種別、残高、証拠金、メモを含めない
- 一括株価更新では、登録されているティッカーの集合だけを送る
- 公開版でAPIプロキシを使う場合は、プロキシ側でも入力データ全体をログに残さない
- 旧 `externalFetchEnabled` や類似フラグは、JSONバックアップから復元されても価格取得可否の判定に使わない
- 価格取得に失敗した場合は、既存の株価・為替値を破壊しない

表示文言:

```text
株価・為替更新時は、銘柄ティッカーまたはUSD/JPY取得リクエストだけを外部サービスへ送信します。
取得できない場合は、Saxo画面の値を手入力してください。
```

### 8.2 建玉ダッシュボード

列に `口座` と `次にやること` を追加する。

```text
銘柄 | 状態 | 口座 | 戦略 | 権利行使価格 | 満期 | プレミアム | 使用分母 | 年率 | 警告 | 次にやること | 操作
```

N口座建玉では、USD実額を主表示し、JPYは参考表示にする。

`次にやること` は、警告とは別に表示する。これはエラーや注文前NGではなく、状態変更後に利用者が次に入力・確認すべき実務タスクである。

例:

```text
約定確認へ
決済実績を入力
株式取得を記録
株式譲渡を記録
満期終了を確認
反対売買判断
入力完了
```

ボタンをクリックすると、該当するカードを開き、その位置へスクロールする。カードが折りたたまれている場合は開く。可能なら最初の未入力欄へフォーカスする。

状態チップ:

- `注文前`: グレー
- `建玉中`: 緑
- `約定情報未確認`: オレンジ
- `決済済み`: 青グレー
- `権利行使済み`: 紫
- `満期終了`: グレー
- `入力未完了あり`: オレンジ枠

色だけに依存せず、必ず文字も表示する。

### 8.3 口座全体カード

REALではP/N別に表示する。

```text
P口座: JPY現金残高 / 証拠金使用率
N口座: USD現金残高 / 証拠金使用率 / 参考JPY換算
```

P/Nを合算した警告判定はしない。

DEMOではN口座欄を出さず、`DEMO / JPYベース` としてJPYベースの口座情報だけを表示する。

#### 8.3.1 決済実績の現金残高反映

口座全体カードの `現金残高` は、原則としてSaxo画面下部から利用者が転記する口座スナップショットである。決済実績を入力しても、アプリが自動で勝手に現金残高へ加算・減算してはいけない。Saxo画面を見て転記した残高と二重計上になる可能性があるためである。

ただし、利用者が明示的に反映したい場合に限り、口座カードに `未反映の現金増減` を表示し、`現金残高に反映` ボタンを置く。

この表示は、入力不備のエラーではない。利用者がSaxo画面の現金残高をそのまま手入力で更新する運用なら、未反映のままでもよい。したがって、文言は「必ず入力してください」ではなく、「現金残高へ反映する場合だけ必要です」とする。

反映に使う金額:

- P口座 / DEMO_JPY_BASE: Saxo取引履歴の `記帳額 (JPY)` を使う
- N口座 / PROD_N_USD_SETTLEMENT: USDの買戻しコストとUSD手数料からUSD現金増減を使う
- `実現損益 (JPY)` は税務・成績用であり、現金残高へ反映する金額として使わない

例:

```text
P口座の買い決済:
実現損益 (JPY): +15,491  -> 税務・成績・年率に使う
記帳額 (JPY): -2,461     -> 現金残高へ反映する場合に使う
```

二重反映防止:

- 反映ボタンを押したら `AccountState.cashAdjustments` に `sourceExecutionId` と金額を記録する
- 同じ決済実績IDは二度反映できない
- 反映後にSaxo画面の最新残高を手入力する場合は、利用者が二重反映にならないよう注意文を表示する
- 現金残高へ反映するために必要な `記帳額 JPY` が未入力のP口座決済では、`現金残高に反映` ボタンを出さず、`記帳額JPYを入力` ボタンを表示する
- N口座でUSD決済情報が不足している場合は、P口座用の `記帳額JPY` という表現を使わず、`決済情報を入力` と表示する
- 不足情報の表示は1回にまとめる。同じ説明文を通常文と赤字警告の2列で重複表示しない
- `記帳額JPYを入力` または `決済情報を入力` を押したら、該当する建玉を選択し、建玉入力を開き、対象の `7. 決済実績` カードへスクロールする
- P口座の `記帳額JPYを入力` では、対象決済実績の `照合用の詳細` を開き、`記帳額 JPY` 入力欄へフォーカスする
- 対象決済実績が複数ある場合は、警告の `sourceExecutionId` に一致するカードだけをハイライトする

表示例:

```text
未反映の現金増減
NVDA P 200 2026-06-05 / 2026-06-02
-2,461円
[現金残高に反映]
```

P口座で記帳額が未入力の場合の表示例:

```text
未反映の現金増減
NVDA P 200 2026-06-05 / 2026-06-02
現金残高へ反映する場合は、Saxo履歴の記帳額JPYが必要です。反映しない場合は未入力のままで問題ありません。
記帳額未入力
[記帳額JPYを入力]
```

N口座でUSD決済情報が不足している場合の表示例:

```text
未反映の現金増減
NVDA P 200 2026-06-05 / 2026-06-02
N口座の現金残高へ反映するための決済情報が不足しています。
情報不足
[決済情報を入力]
```

入力欄は、SaxoTraderGO画面下部の並びに合わせる。

DEMO / JPYベース、P口座:

```text
現金残高（未受渡分込み）
買付可能額
口座純資産
必要証拠金余力
証拠金使用率
```

N口座:

```text
USD現金残高
USD買付可能額
USD口座純資産
必要証拠金余力
証拠金使用率
参考JPY換算
```

表示しない・使わないラベル:

```text
必要証拠金（JPY）
必要証拠金（USD）
```

理由:

- Saxo画面の第一面に表示されるのは `必要証拠金余力`
- `必要証拠金` と表示すると、ユーザーがSaxo画面で該当項目を探せず入力しづらい
- アプリのリスク判定では、口座全体の証拠金状態は `証拠金使用率` と `必要証拠金余力` で確認する
- 個別建玉の使用証拠金は、別途 `チケット表示証拠金 × 証拠金バッファ` で扱う

### 8.4 建玉入力

最初に口座を選ぶ。

```text
口座:
P: 外国株式等オプション（円建て / JPY決済）
N: 外国株式等オプション（ドル建て / USD決済）
```

口座に応じて入力ラベルを変える。

P口座:

- JPY決済額
- 適用為替
- JPY手数料（画面上でJPY手数料が確認できる場合だけ入力）
- 取引手数料（USD）

N口座:

- USDプレミアム
- 取引手数料（USD）
- 参考USD/JPY

新規建玉時の既定値:

- 取引手数料（USD）: `2.25 USD`
- JPY手数料: Saxo画面上でJPY手数料が確認できる場合だけ入力する補助的な任意入力
- NISA等 比較年率: `9%`
- 建玉日 / 取引日など、新規に作成する実績入力のイベント日付: 本日

理由:

- Saxoのオプションチケットでは取引手数料が `2.25 USD` のようにUSDで表示されるため、建玉時の手数料初期値は取引手数料（USD）を主に使う
- JPY手数料はSaxo画面に明示されない場合があるため、補助的な任意入力として扱い、既定値では主計算へ強制しない
- NISA等比較年率は、ユーザーが比較対象として使う前提値を `9%` から開始する

#### 8.4.0 日付入力の既定値

新規に入力を始める時、利用者が毎回日付を直す負担を減らすため、イベント日付は原則として本日を初期表示する。

ここでいう本日とは、ブラウザのローカル日付である。UTCの `toISOString().slice(0, 10)` をそのまま使うと、日本時間の朝や深夜に日付がずれる可能性があるため、実装ではローカルタイムゾーンで `YYYY-MM-DD` を作る共通関数を使う。

対象にする日付:

- 新規建玉の `建玉日`
- 建玉約定確認の `取引日`
- 決済実績の `決済日 / 取引日`
- 満期終了記録の `記録日 / 実現日`
- 権利行使済みにした後の `株式取得日`
- C売り権利行使や現物売却の `譲渡日`
- P口座からN口座への移管記録の `移管日`
- 円転・資金振替などの `取引日`
- 手入力で追加する履歴レコードの作成日

対象にしない日付:

- オプションの `満期日`
- オプションチェーンやSaxoチケットに表示される契約上の期限
- 既に保存済みの建玉日、決済日、譲渡日、移管日
- JSON復元で読み込んだ日付

理由:

- `建玉日`、`決済日`、`取引日` は「ユーザーがこれから入力する実績イベントの日付」であり、多くの場合は入力当日になる
- `満期日` はオプション契約そのものの条件であり、本日に寄せると契約情報を壊す
- 既存データをアプリ起動時に今日へ上書きすると、履歴と年率計算が壊れる

状態変更時の既定値:

- `注文前` から `建玉中` へ変更して建玉約定確認の下書きを作る場合、下書きの `tradeDate` は本日にする
- `建玉中` から `決済済み` へ変更して決済実績の下書きを作る場合、下書きの `closeDate` は本日にする
- `建玉中` から `満期終了` へ変更する場合、契約上の `満期日` は変更しない。満期終了記録の実現日は本日を初期表示し、実際の満期日で記録したい場合は利用者が変更できるようにする
- `建玉中` から `権利行使済み` へ変更する場合、株式取得記録または譲渡記録のイベント日は本日にする

実装上の注意:

- 日付初期値は「新規作成時にフィールドが空の場合」だけ入れる
- ユーザーが一度編集した日付を、戦略変更、口座変更、カード開閉、保存/復元で上書きしない
- Saxo read-only版でSaxoから実際の取引日が取得できる場合は、Saxoの日付を優先する。取得できない場合だけ本日を使う
- 公開版、ローカル版、グレードアップ版で同じ日付既定値ルールにする

### 8.4.1 C売り分類

C売りは、現物株を保有しているカバードコールと、現物株を保有していないネイキッドコールを混同しない。

従来の `C売りの方針` は、現物株を保有しているカバードコール前提では次の2択でよい。

```text
1. 株を残したい
2. 株を売却されてもよい
```

ただし、ショートストラングルなどで現物株を保有せずにコールを売る場合は、上記2択では不適切である。未カバーのC売りには、次の分類を追加する。

```text
3. 現物なし・上抜け時は買戻し
```

UI補足文:

```text
現物株を保有せずにコールを売る前提です。株価が想定ラインを上抜けた場合は、逆指値などで買い戻して撤退する方針を確認します。急騰時や時間外の値飛びでは、想定より不利な価格で約定する可能性があります。
```

判定:

```text
coveredCallShares = 保有株数
shortCallShares = C売り枚数 * 100
coveredShares = min(coveredCallShares, shortCallShares)
nakedCallShares = max(0, shortCallShares - coveredCallShares)
```

- `nakedCallShares = 0`: カバードコール扱い。`株を残したい` / `株を売却されてもよい` を表示する
- `nakedCallShares > 0`: 未カバー部分をネイキッドコール扱いにする
- ネイキッドコールが含まれる場合は、`現物なし・上抜け時は買戻し` を選べるようにする
- 現物株が一部だけ足りない場合は、カバー済み部分と未カバー部分を分けて注意表示する

避ける表現:

- `現物なしで上値リスクを取る`
- `逆指値で損失限定`
- 現物がないC売りに対する `株を売却されてもよい`

ネイキッドコールで入力または確認する項目:

- 逆指値ライン
- 買戻し価格ライン
- 許容損失額
- 満期何日前までに判断するか
- 逆指値でもギャップ・流動性不足で想定より不利に約定する可能性の確認

### 8.5 出口ルール・表示設定

サクソバンク証券では、決済注文、IFD、IFD-OCO、OCO等により、オプション建玉の利確ラインまたは損切りラインを事前に設定できる。アプリでは、これを「発注機能」ではなく、Saxoへ入力する出口注文の設計メモとして扱う。

対象:

- プット売り
- カバードコール
- カバードコール＋追加P売り
- ショートストラングル
- ホイール中の各売り建てオプション

特に `P売りの方針 = 株を取得したくない` の場合は、満期放置ではなく、注文前に反対売買の出口を決める前提で扱う。

入力項目:

```ts
type ExitOrderPlan = {
  scope: "position" | "leg";
  legId?: string;
  mode:
    | "manual_only"
    | "after_entry_closing_order"
    | "attached_entry_exit_order";
  brokerOrderType?:
    | "none"
    | "closing_limit"
    | "closing_stop"
    | "oco"
    | "ifd"
    | "ifd_oco";
  profitTakeEnabled: boolean;
  profitTakePremiumKeepPercent?: number;
  profitTakeBuybackPriceUSD?: number;
  stopLossEnabled: boolean;
  stopLossType?: "buyback_price" | "stock_price_line" | "loss_amount";
  stopLossBuybackPriceUSD?: number;
  stopLossStockPriceUSD?: number;
  stopLossAmountJPY?: number;
  stopLossAmountUSD?: number;
  stopLossAmountCurrency?: "JPY" | "USD";
  latestCloseDaysBeforeExpiry?: number;
  latestCloseDaysBeforeExpiryUserSet?: boolean;
  memo?: string;
};
```

出口ルールは、原則として「建玉全体」ではなく「売り建てオプション脚ごと」に適用する。カバードコール＋追加P売りのようにC売りとP売りが同時に存在する場合、C脚とP脚では出口判断の意味が異なるため、同じ利確・損切り・満期前タイムリミットを無条件に両方へ表示しない。

脚ごとの扱い:

```text
P売り:
- 株を取得したくない場合、出口ルールは重要
- 利確、損切り、満期前タイムリミット、Saxo出口注文の使い方を表示する
- 反対売買判断では、現在のP買戻し価格を入力して判定する

C売り:
- 株を売却されてもよい場合、買戻し判断は通常不要
- 反対売買判断では「通常は買戻し不要。株を残したい場合だけ入力」と表示する
- 利確・損切り・満期前タイムリミットをP売りと同じ条件で自動表示しない

C売りで株を残したい場合:
- C脚専用の出口ルールを表示する
- 現在のC買戻し価格を入力して、株を残すための買戻しコストを確認する
- 出口ルールカードは単なるメモ欄にせず、反対売買判断のC買戻し価格入力へ誘導する

C売りで現物なし・上抜け時は買戻しの場合:
- ネイキッドコールまたは未カバー部分があるC売りとして扱う
- 上方向の損失リスクを明示する
- 逆指値ライン、買戻し価格ライン、許容損失額、満期前タイムリミットを表示する
- `逆指値あり` でも損失限定とは表示しない
- 急騰、時間外の値飛び、寄付きギャップ、流動性不足で想定より不利に約定する可能性を警告する
```

損失額ルールの通貨:

- P口座、DEMO / JPYベース: `stopLossAmountJPY` を主入力にする
- N口座: `stopLossAmountUSD` を主入力にする
- JPY換算は参考表示に留める
- N口座で `stopLossAmountJPY` をUSD判定に流用しない

UI上は `4. ルール・表示設定` を、次の2カードに分離する。

```text
4. 出口ルール
5. 表示・比較設定
```

`出口ルール` は、Saxoに置く出口注文、または手動判断用の利確・損切り基準だけを扱う。

`表示・比較設定` は、年率計算・分母比較・NISA等比較の前提だけを扱う。主分母とNISA等比較年率は出口注文とは別概念なので、出口ルールカード内に置かない。

表示する項目:

#### 4. 出口ルール

- Saxo出口注文の使い方
  - 使わない（手動判断）
  - 建玉後に決済注文を入れる
  - 新規注文と同時にIFD/OCOを入れる
- Saxo注文タイプ
  - なし
  - 決済指値
  - 決済逆指値
  - OCO
  - IFD
  - IFD-OCO
- 利確ルール
  - プレミアム50%確保
  - プレミアム60%確保
  - プレミアム80%確保
  - 任意%
- 利確買戻し価格
- 損切りルール
  - 買戻し価格
  - 株価ライン
  - 損失額
- 損切り買戻し価格または株価ライン
- 満期何日前までに決済判断するか
- Saxo決済注文メモ

出口ルールカードでは、売り脚が複数ある場合に脚ごとに表示を分ける。

表示例:

```text
P 80 2026-07-02
方針: 株を取得したくない
出口ルール: 必須に近い管理対象
利確: 60%確保
損切り: 損失額 6,400円
満期前判断: 14日前

C 95 2026-07-02
方針: 株を売却されてもよい
出口ルール: 通常は不要
株を残したい場合だけ、C買戻し価格を反対売買判断で確認

C 95 2026-07-02
方針: 現物なし・上抜け時は買戻し
出口ルール: 上抜け時の撤退ルールが必要
上抜けライン: 100 USD
損切り: 買戻し価格または許容損失額で管理
注意: 逆指値でもギャップ時は想定より不利に約定する可能性
```

カバードコール＋追加P売りの場合、P売り用に設定した利確・損切り・満期前タイムリミットをC売りへ自動適用しない。

C売りで `株を残したい` 場合の出口ルールカードは、次の目的で使う。

- 株を残すために、C売りを買い戻す可能性があることを明示する
- Saxoの決済チケットで確認すべき値が `C買戻し価格` であることを示す
- 反対売買判断カードの該当C脚へ移動する導線を置く
- 満期前の判断期限を設定し、期限到達時に警告を出す
- 利益確保そのものよりも、株を残すためにいくら払って閉じるかを確認する

表示例:

```text
C売り 95 2026-07-02
方針: 株を残したい

このC売りは、株を残すために買戻し判断を行う可能性があります。
Saxoの決済チケットでC買戻し価格を確認し、反対売買判断に入力してください。

[反対売買判断でC買戻し価格を入力する]
満期何日前までに判断: 7日前
```

このカードは、単なるメモ欄ではなく、判断期限と反対売買判断への導線を持つカードとする。

使い方:

1. 利用者が `C売りの方針 = 株を残したい` を選ぶ
2. 出口ルールカードに、C脚専用の判断期限と `反対売買判断へ` ボタンを表示する
3. 利用者はSaxoの決済チケットでC買戻し価格を確認する
4. `反対売買判断` のC脚カードへ入力し、株を残すための買戻しコストを確認する

したがって、このカードは「メモ用」ではない。Saxoに自動発注する機能でもない。手入力版では、C買戻し判断を忘れないための設定・導線・期限警告の役割を持つ。

#### 5. 表示・比較設定

- 主分母
- NISA等 比較年率

主分母は分母比較・年率換算の前提であり、出口注文の設定ではない。画面上も出口ルールとは分ける。

UIでは `Saxoで決済注文を設定する` と `建て注文に出口を添付する` の2つのチェックボックスを並べない。利用者には違いが分かりにくいため、1つの選択項目 `Saxo出口注文の使い方` に統合する。

選択肢の意味:

```text
使わない（手動判断）
= Saxoには出口注文を置かず、アプリの利確・損切りルールを見ながら手動判断する

建玉後に決済注文を入れる
= すでに建てた建玉に対して、あとから決済指値・決済逆指値・OCOをSaxoに置く

新規注文と同時にIFD/OCOを入れる
= これから建てる注文と同時に、利確・損切りの出口注文もSaxoに添付する
```

利確買戻し価格は、受取プレミアムと確保率から自動計算できる。

```text
利確買戻し価格 = 建てた時のプレミアム × (1 - プレミアム確保率)
```

例:

```text
建てた時のプレミアム 1.00 USD
80%利確
→ 利確買戻し価格 0.20 USD
```

満期前タイムリミットは、買いたくない・買えないプット売りでは重要項目として扱う。標準候補は `満期7日前` とする。ただし、利用者が `14日前` などに変更できるようにする。

`株を取得したくない` プット売りでは、次のどれかが未設定なら注意または注文前NG候補にする。

- 利確ルール
- 損切りルール
- 満期前タイムリミット
- Saxoで決済注文/IFD-OCOを使うか、手動監視にするかの選択

`株を取得してもよい` プット売りでは、上記を必須NGにはしない。ただし、任意の管理ルールとして設定できる。

### 8.5.1 Saxo注文タイプと利確・損切り表示の連動

`利確ルールを使う` と `損切りルールを使う` の2つのチェックボックスを、Saxo注文タイプと独立して並べると利用者が混乱する。原則として、Saxo注文タイプから表示・有効化するルールを自動決定する。

連動ルール:

```text
決済指値
→ 利確ルールのみ表示・有効化
→ 損切りルールは非表示または無効

決済逆指値
→ 損切りルールのみ表示・有効化
→ 利確ルールは非表示または無効

OCO
→ 利確ルールと損切りルールの両方を表示・有効化

IFD-OCO
→ 利確ルールと損切りルールの両方を表示・有効化

使わない（手動判断）
→ Saxo注文としては使わない
→ アプリ内の判断基準として、利確ライン・損切りラインを任意表示できる
```

チェックボックスを残す場合でも、文言は `利確ラインを表示する`、`損切りラインを表示する` とし、Saxoへ注文を置く意味に見えないようにする。

ただし推奨UIは、チェックボックスを主操作にせず、Saxo注文タイプに応じて必要な入力欄だけを出す方式である。

出口ルールカードの説明文:

```text
ここでは、Saxoに置く出口注文、または手動判断用の利確・損切り基準を決めます。途中決済時の現在価格は、下の反対売買判断に入力します。
```

反対売買判断側の説明文:

```text
Saxoの決済チケットに表示される現在の買戻し価格を入力し、出口ルールに到達しているか確認します。
```

### 8.5.2 満期前タイムリミット警告

`満期何日前までに決済判断するか` は、APIで現在プレミアムを取得できない手入力版でも警告に使える。

警告条件:

```text
latestCloseDaysBeforeExpiry が設定されている
かつ 現在日付 >= 満期日 - latestCloseDaysBeforeExpiry
かつ 建玉状態が planned または open
かつ closed / assigned / expired ではない
```

警告文例:

```text
満期前の判断期限に到達しています。反対売買判断で現在の買戻し価格を確認してください。
```

`株を取得したくない` プット売りでは、満期前タイムリミット超過を強めの警告にする。

警告の表示場所:

- 建玉ダッシュボードの警告列
- サマリーカードの `最大注意点`
- リスク警告カード
- 該当する出口ルールカード内

警告は、単なる文字表示で終わらせない。警告には `反対売買判断へ` ボタンまたはクリック可能なリンクを付ける。クリック時は、反対売買判断カードを自動で開き、該当する脚の入力欄までスクロールする。

脚別の警告文例:

```text
P売り・株を取得したくない:
満期前の判断期限に到達しています。P買戻し価格を反対売買判断で確認してください。

C売り・株を残したい:
株を残したい方針のC売りです。判断期限に到達しています。C買戻し価格を反対売買判断で確認してください。

C売り・現物なし・上抜け時は買戻し:
現物なしのC売りです。判断期限に到達しています。上抜け時の買戻し価格または逆指値ラインを反対売買判断で確認してください。
```

クリック先:

```text
P売り警告 → close-decision-put-{legId}
C売り警告 → close-decision-call-{legId}
```

クリック後の挙動:

- 反対売買判断カードが畳まれている場合は開く
- 該当脚の入力欄が見える位置までスクロールする
- 可能であれば、現在の買戻し価格入力欄へフォーカスする

### 8.6 反対売買判断

反対売買判断は、現在のSaxo決済チケット価格を入力し、出口ルールに到達しているかを確認する領域とする。発注機能ではない。

表示する項目:

- 建てた時のプレミアム
- 現在の買戻し価格
- 建てた時の手数料
- 買戻し時の想定手数料
- 今閉じた場合の概算損益
- 今閉じた場合の年率
- プレミアム確保率
- 利確ルール判定
- 損切りルール判定
- 満期前タイムリミットまで残り何日
- Saxoで設定する決済指値・決済逆指値の目安

反対売買判断も脚ごとに表示内容を変える。

P売り:

- P買戻し価格入力欄を表示
- 利確ルール判定を表示
- 損切りルール判定を表示
- 満期前タイムリミットを表示
- Saxo決済注文目安を表示

C売りで `株を売却されてもよい`:

- C買戻し価格入力欄は折りたたむ、または任意表示にする
- 利確ルール判定、損切りルール判定、満期前タイムリミットをP売りと同じ条件で表示しない
- 説明文を表示する

```text
このC売りは、株を売却されてもよい方針です。通常は買戻し判断を行いません。株を残したい場合だけ、買戻し価格を入力してコストを確認してください。
```

C売りで `株を残したい`:

- C買戻し価格入力欄を表示
- C脚専用の買戻し判断として表示
- P脚用の損切りルールや満期前タイムリミットを流用しない
- 出口ルールカードから該当C脚の入力欄へジャンプできるようにする

C売りで `現物なし・上抜け時は買戻し`:

- C買戻し価格入力欄を表示
- 上抜けライン、買戻し価格ライン、許容損失額の判定を表示
- ネイキッドコールまたは未カバー部分の上方向リスクを表示
- `逆指値で損失限定` とは表示しない
- 逆指値を設定していても、急騰、時間外の値飛び、寄付きギャップ、流動性不足で想定より不利に約定する可能性を表示する
- P売り用の買受資金チェックとは分けて表示する

反対売買判断カードには、脚ごとに安定したアンカーIDを付ける。

```text
close-decision-call-{legId}
close-decision-put-{legId}
```

出口ルールカードや警告から該当アンカーへスクロールできるようにする。

判定例:

```text
建てた時のプレミアム: 1.07 USD
利確ルール: 60%確保
利確買戻し価格: 0.43 USD以下
現在の買戻し価格: 0.40 USD
判定: 利確ライン到達
```

損切り例:

```text
損切りルール: 買戻し価格 2.00 USD以上
現在の買戻し価格: 1.80 USD
判定: 未到達
```

反対売買判断で入力した買戻し価格は、出口ルール判定と同期する。建玉入力欄と反対売買判断欄で同じ意味の入力を重複させない。入力場所は反対売買判断側を主とする。

#### 8.6.1 途中決済時の年率表示

反対売買判断では、現在の買戻し価格を入力した時点で、`今閉じた場合の概算損益` だけでなく `今閉じた場合の年率` も表示する。

目的:

- プレミアム確保率だけでは、資金効率が分からない
- 途中決済で実際に得られる損益を、使用した期間と分母に対して年率換算する
- 満期まで保有した場合の年率と、途中決済した場合の年率を区別する

計算式:

```text
途中決済年率 =
  今閉じた場合の概算損益（手数料後）
  ÷ 使用分母
  × (365 ÷ 建玉日から反対売買判断日までの実経過日数)
  × 100
```

日数:

```text
holdingDays = max(1, 反対売買判断日 - 建玉日)
```

- 満期日までの日数ではなく、建玉日から反対売買判断日までの実経過日数を使う
- 当日中の判断では0日割りを避けるため、最低1日として扱う
- 反対売買判断日は、通常は今日の日付を使う
- 将来的に決済日入力を追加する場合は、その決済日を使う

分母:

- 画面上で選択されている主分母を使う
- P口座、DEMO / JPYベースではJPY損益 ÷ JPY分母
- N口座ではUSD損益 ÷ USD分母を主計算にし、JPYは参考表示にする

表示例:

```text
今閉じた場合の概算損益（手数料後）: +15,415円
今閉じた場合の年率: 123.4%
計算: 15,415円 ÷ 使用分母 × (365 ÷ 7日)
```

注意:

- これは途中決済時点の概算年率であり、満期まで保有した場合の年率とは別物
- 買戻し価格や手数料を入力していない場合は `未計算` と表示する
- 損益がマイナスの場合は、途中決済年率もマイナス表示にする

#### 8.6.1.5 建玉約定確認

`注文前` と `建玉中` では、入力値の意味が変わる。

- `注文前`: Saxoの注文チケットを見ながら入れる予定値、または発注前の試算値
- `建玉中`: Saxoで実際に約定した後の建玉実績値

そのため、建玉状態を `注文前` から `建玉中` に変更した時は、`オプション脚` の入力欄をそのまま放置せず、`建玉約定確認` カードへ自動で移動する。

目的:

- 注文前の予定値を、約定後の正しい値へ確認・修正する
- 利用者がどこを直せばよいか迷わないようにする
- 予定値ベースの計算と、実績値ベースの計算を混同しない

追加するデータ型:

```ts
type OptionEntryExecution = {
  id: string;
  legId: string;
  tradeDate: string;
  contracts: number;
  fillPriceUSD: number;
  settlementCurrency: "JPY" | "USD";
  brokerBookedAmountJPY?: number;
  brokerPremiumJPY?: number;
  brokerTransactionCostJPY?: number;
  brokerNetPremiumJPY?: number;
  brokerFeeJPY?: number;
  brokerExchangeFeeJPY?: number;
  brokerExchangeConversionFeeJPY?: number;
  brokerExchangeFeeExchangeJPY?: number;
  brokerFeeConsumptionTaxJPY?: number;
  brokerExchangeRateJPY?: number;
  commissionUSD?: number;
  commissionJPY?: number;
  referenceFxRateJPY?: number;
  inputMode?: "P_JPY_BROKER_STATEMENT" | "USD_EXECUTION_CALC";
  source: "manual" | "broker_statement" | "saxo_api_estimate";
  confirmed: boolean;
  memo?: string;
};

type TradeSimulation = {
  optionEntryExecutions?: OptionEntryExecution[];
};
```

注文ID、ポジションID、約定時刻は通常入力に出さない。日常の損益管理・年率計算・税務区分集計には不要であり、UIを複雑にするためである。必要な場合はメモ欄に任意で書ける程度にとどめる。

`注文前` から `建玉中` へ変更した時の自動導線:

- 売り建てオプション脚ごとに `OptionEntryExecution` 下書きを自動作成する
- 既に対象脚の下書きがある場合は重複作成しない
- 現在のオプション脚入力値を初期値としてコピーする
  - `tradeDate`: 本日。ただしSaxo read-onlyなどで実際の取引日を取得できる場合はSaxoの日付を優先する
  - `contracts`: 対象脚の数量
  - `fillPriceUSD`: 対象脚のプレミアムUSD
  - `settlementCurrency`: 口座分類に応じて `JPY` または `USD`
  - `brokerExchangeRateJPY`: P口座 / DEMOなら現在の為替
  - `commissionUSD`: N口座のみ既定値 `2.25`。P口座 / DEMOでは通常入力に出さない
  - `source`: `manual`
  - `confirmed`: `false`
- `建玉約定確認` カードを自動で開き、画面をスクロールする
- P口座 / DEMOでは `記帳額 (JPY)` を最小入力の中心にする。`プレミアム (JPY)` と `取引費用 (JPY)` は確認・内訳用として同じカード内に置く。`為替レート`、詳細内訳は折りたたみまたは補助欄にする
- N口座では `USD約定価格`、`USD手数料`、`USD実現損益/プレミアム` を入力しやすく強調する
- 利用者が確認を終えたら `約定情報を確認済みにする` ボタンで `confirmed: true` にする

P口座 / DEMO_JPY_BASE の建玉約定入力:

Saxo取引履歴では、建玉時に次のような情報が見える。

```text
売 / 建玉
価格
数量
取引日
記帳額 (JPY)
取引費用 (JPY)

詳細:
プレミアム
手数料
為替変換手数料
取引所手数料
手数料・消費税額
為替レート
```

P口座の最小入力欄:

- 対象脚: 自動選択
- 取引日: Saxo履歴の取引日。新規下書きでは本日を初期表示し、Saxo履歴の日付に修正できるようにする
- 約定価格 USD: Saxo履歴の価格。注文前のプレミアムUSDから初期入力
- 数量: Saxo履歴の数量。注文前の数量から初期入力
- 記帳額 JPY: Saxo履歴の行に出る `記帳額 (JPY)`
- プレミアム JPY: Saxo詳細の `プレミアム`
- 取引費用 JPY: Saxo詳細の `取引費用`。記帳額を再計算するための必須欄ではなく、費用確認用
- メモ

P口座の補助欄:

- 為替レート
- 詳細内訳: 手数料 JPY、為替変換手数料 JPY、取引所手数料 JPY、手数料・消費税額 JPY

補助欄は初期表示で大きく出さない。`詳細を開く` で表示する。

P口座では `取引手数料 USD` を通常入力に出さない。P口座のSaxo履歴では手数料・費用はJPYで表示されるため、USD手数料を残すと二重入力・二重控除の原因になる。

P口座の主計算:

```text
建玉時の現金反映額 JPY = 記帳額 JPY
```

`手数料控除後受取額 JPY` という表現は使わない。Saxo画面上の意味に合わせ、`記帳額 JPY` または `現金反映額 JPY` と表記する。

`取引費用 JPY` は、Saxo画面のように `-541 JPY` とマイナスで入力しても、`541` と正で入力してもよい。内部では費用情報として正規化する。ただし、`記帳額 JPY = プレミアム JPY - abs(取引費用 JPY)` として一律に計算してはいけない。

記帳額との関係:

- `記帳額 JPY` は、P口座の建玉時キャッシュフローとして最優先する
- `プレミアム JPY` は、受取プレミアムの確認用である
- `取引費用 JPY` は、費用確認用である
- Saxoでは `記帳額 JPY` と `取引費用 JPY` の控除範囲が完全一致しない場合があるため、`取引費用 JPY` から `記帳額 JPY` を再計算しない
- 例: プレミアム 18,308 JPY、記帳額 17,952 JPY、取引費用 -541 JPY の場合、アプリの現金反映額は 17,952 JPY であり、18,308 - 541 = 17,767 JPY とはしない

自動計算:

- `記帳額 JPY` が入力されたら、`現金反映額 JPY` としてそのまま表示する
- `プレミアム JPY` と `記帳額 JPY` が入力されている場合、`記帳額との差額 = プレミアム JPY - 記帳額 JPY` を参考表示してもよい
- `取引費用 JPY` が入力されている場合は、差額との比較を参考表示する。ただし不一致を計算エラーにしない
- `記帳額 JPY` が未入力の場合だけ、`プレミアム JPY` と費用内訳から `概算記帳額` を参考表示してよい。その場合は必ず `概算` と明示する

Saxo画面で費用がマイナス表示される場合、入力欄はマイナス入力も許容する。ただし内部計算では「費用」として絶対値または正のコストに正規化し、二重控除しない。

P口座では、SaxoのJPY実績を優先する。

- `プレミアム JPY` と `取引費用 JPY` が入力されている場合、建玉時の純受取額はそれを優先する
- 未入力の場合だけ、USDプレミアム × 為替から概算する
- 概算値を使う場合は `概算` と表示する
- Saxo実績値を使う場合は `Saxo実績ベース` と表示する

N口座 / PROD_N_USD_SETTLEMENT の建玉約定入力:

N口座はUSDが主帳簿であるため、建玉約定入力はUSDベースを主にする。

- 対象脚
- 取引日
- 約定価格 USD
- 数量
- USD手数料
- USDプレミアム
- 参考為替
- 参考JPY換算
- メモ

N口座ではJPY欄を主入力にしない。

未確認時の扱い:

- `建玉中` だが `OptionEntryExecution.confirmed` が `false` の場合、ダッシュボードまたは該当カードに `約定情報未確認` を表示する
- これは注文前NGではなく、実績入力の注意である
- 計算は既存のオプション脚値で継続できるが、`予定値ベース` または `約定未確認` と表示する
- 警告をクリックすると `建玉約定確認` カードへスクロールする

#### 8.6.1.6 建玉状態ワークフロー・ナビゲーション

建玉状態の変更は、単なるプルダウンではなく、次に必要な入力へ進むワークフローとして扱う。

目的:

- 利用者が「どこで状態を変えるか」を一目で分かるようにする
- 状態変更後に必要な実績入力へ迷わず移動できるようにする
- 入力漏れ、予定値と実績値の混同、履歴集計ミスを減らす

追加する派生型:

```ts
type WorkflowTaskType =
  | "confirm_entry_execution"
  | "enter_close_execution"
  | "confirm_expiry"
  | "enter_stock_acquisition"
  | "enter_stock_settlement"
  | "review_close_decision"
  | "complete";

type WorkflowTask = {
  id: string;
  simulationId: string;
  type: WorkflowTaskType;
  severity: "info" | "warning" | "danger";
  label: string;
  detail: string;
  actionLabel: string;
  targetAnchor:
    | "option-entry-executions"
    | "option-close-executions"
    | "stock-acquisition"
    | "stock-settlement"
    | "close-decision"
    | "simulation-editor";
  focusField?: string;
};
```

`WorkflowTask` は原則として保存データではなく、`TradeSimulation` から都度計算する派生データとする。JSONバックアップに保存しない。

タスク判定:

| 状態 | 条件 | 次にやること | targetAnchor |
| --- | --- | --- | --- |
| `注文前` | 注文前 | 注文内容を確認 | `simulation-editor` |
| `建玉中` | `optionEntryExecutions` が未作成または未確認 | 約定確認へ | `option-entry-executions` |
| `建玉中` | 約定確認済み | 反対売買判断 / 満期待ち | `close-decision` |
| `決済済み` | 決済実績なし、または未完成 | 決済実績を入力 | `option-close-executions` |
| `満期終了` | 満期終了履歴なし、または未確認 | 満期終了を確認 | `option-close-executions` |
| `権利行使済み` | P売りの株式取得記録なし | 株式取得を記録 | `stock-acquisition` |
| `権利行使済み` | C売りの株式譲渡記録なし | 株式譲渡を記録 | `stock-settlement` |
| 結果状態 | 必要記録がすべて完了 | 入力完了 | なし |

ダッシュボード表示:

- `次にやること` 列には、その建玉の最優先タスクを1つ表示する
- 複数タスクがある場合は `未完了2件` のように表示し、クリックで詳細を開く
- `danger` は赤、`warning` はオレンジ、`info` は青またはグレーで表示する
- ただし `info` は警告件数には含めない

ワークフロー判定の優先順位:

1. 建玉開始の約定確認が未確認なら、最優先で `約定確認へ` を出す
2. 建玉開始の約定確認が完了し、確認済みの決済実績がないなら、`反対売買判断` を出す
3. 決済実績の下書きがあるが、まだ確認済みでない場合は、`決済実績を確認` を出す
4. 決済実績が確認済みになった場合だけ、`決済済みに変更` を出す
5. 満期終了記録が確認済みになった場合だけ、`満期終了に変更` を出す

`反対売買判断` と `決済実績` を混同しないため、反対売買判断で入れた買戻し価格や、そこからコピーされた下書きだけをもって `決済済みに変更` を出してはいけない。`決済済みに変更` は、Saxo注文履歴を見て実績入力を確認済みにした後だけ出す。

状態変更UI:

`建玉状態` は、フォームの途中に埋め込むだけではなく、編集画面上部に `状態ステッパー` として表示する。

```text
注文前 -> 建玉中 -> 決済済み / 権利行使済み / 満期終了
```

- 現在状態を色付きで強調する
- 次に選べる状態をボタン化する
- 状態変更のプルダウンは残してもよいが、主導線はステッパーまたは状態変更ボタンにする
- 状態変更ボタンを押した直後に、必要な下書きを作成し、次の入力カードへ移動する

状態変更後の通知:

状態変更後、画面上部またはカード上部に短いガイドを表示する。

`注文前` -> `建玉中`:

```text
建玉中に変更しました。次にSaxoの取引履歴を見ながら約定情報を確認してください。
```

ボタン:

```text
約定確認へ進む
```

`建玉中` -> `決済済み`:

```text
決済済みに変更しました。次にSaxoの注文履歴から決済実績を入力してください。
```

ボタン:

```text
決済実績へ進む
```

`建玉中` -> `権利行使済み`:

```text
権利行使済みに変更しました。必要な株式取得または譲渡の記録を入力してください。
```

`建玉中` -> `満期終了`:

```text
満期終了に変更しました。買戻しなしでプレミアムが確定した履歴を確認してください。
```

ハイライト:

- 自動で移動したカードは、3〜5秒だけ淡いオレンジ枠または青枠で強調する
- 未入力の主項目は淡い背景色を付ける
- 入力完了または確認済みにしたら通常色へ戻す
- ハイライトは画面再読み込み後まで保存しない

状態変更ガイドの表示寿命:

- `建玉中に変更しました。次にSaxoの取引履歴を見ながら約定情報を確認してください。` は、`注文前` から `建玉中` へ変更した直後だけの一時案内である
- 利用者が `建玉開始を確認済みにする` を押したら、この一時案内は消す
- 建玉開始が確認済みの状態では、同じ案内を出し続けない
- 建玉開始確認後の案内は、必要なら `建玉開始の確認が完了しました。次は反対売買判断または満期管理です。` に切り替える
- ダッシュボードの `次にやること` と、編集画面上部のガイドは同じ `WorkflowTask` 判定に基づく。両者が別々の状態を示してはいけない

バージョン間の表示一致:

- ローカル版、一般公開版、グレードアップ版は、同じ入力データで同じ `WorkflowTask` を返す
- 実装対象ソースは、グレードアップ版を `apps/us-options-risk-planner`、一般公開版を `us-options-risk-planner-public-repo` として扱う。静的配信先や `pages-dist` はビルド成果物であり、原則として直接修正対象にしない
- `workflowTasks.ts`、`optionCloseExecutions.ts`、`SimulationEditor.tsx`、`types/domain.ts`、JSON import/export・移行処理は、グレードアップ版と一般公開版で同じ仕様に揃える
- 表示差が出る場合は、保存済みJSON/localStorage内の `optionEntryExecutions`、`optionCloseExecutions`、`confirmed` 状態、または移行処理の差を疑う
- 公開版だけ `決済済みに変更` が出る場合は、公開版の保存データに決済実績下書きまたは旧形式の決済実績が残っていないか確認する
- 旧形式データの決済実績は、現在状態が `open` の場合は未確認下書きとして扱い、自動で `決済済みに変更` へ進めない
- 既存データ移行では、`OptionCloseExecution.confirmed` が存在しない旧データを、`status === "open"` なら `confirmed: false`、`status === "closed"` または `status === "expired"` なら利用者の既存履歴を壊さないため `confirmed: true` として扱う。ただし `open` のまま決済済みボタンを出すことは避ける

実装方針:

- `getWorkflowTasks(simulation): WorkflowTask[]` をdomain層に作る
- `Dashboard` は `getWorkflowTasks` の最優先タスクを `次にやること` 列に表示する
- `SimulationEditor` は状態変更時に必要な下書きを作成し、`targetAnchor` へスクロールする
- スクロール処理は共通関数 `navigateToWorkflowTask(task)` または同等のUI関数にまとめる
- カードには安定した `id` を付ける
  - `option-entry-executions`
  - `option-close-executions`
  - `stock-acquisition`
  - `stock-settlement`
  - `close-decision`
- 折りたたみカードは、ナビゲーション時に自動で開く

完了条件:

- ダッシュボードだけを見ても、各建玉で次に何をすればよいか分かる
- 状態変更場所が一目で分かる
- 状態変更後、該当入力カードへ自動移動する
- そのカードが一時的に色で強調される
- 未入力欄が分かる
- 入力完了後、次にやることが `入力完了` または次のタスクに変わる

#### 8.6.1.7 入力完了後の状態変更プロンプト

`建玉状態` は最終的に利用者が確認して変更する。ただし、実績カードの入力完了後に次の状態変更を促さないと、利用者は「入力したのにダッシュボードがまだ同じ状態に見える」と感じやすい。

そのため、状態変更は完全自動ではなく、入力完了後に明示的な次アクションボタンを表示する。

基本方針:

- `simulation.status` を唯一の状態ソースにする
- `1. 銘柄・価格` の `建玉状態`、編集画面上部の状態ワークフロー、ダッシュボードの状態表示は同じ `simulation.status` を参照する
- 実績入力カードは、必要情報が揃った時点で状態変更の候補ボタンを出す
- ボタン押下時は既存の状態変更処理を通し、下書き作成、スクロール、通知を一貫させる
- 入力完了だけで勝手に結果状態へ変更しない。利用者が状態変更ボタンを押して確定する

3-A 建玉開始の約定確認:

名称は `3-A. 建玉開始の約定確認` とする。ここは新規建玉がSaxoで実際に約定したことを確認する領域であり、決済済みにする領域ではない。

確認ボタン:

```text
建玉開始を確認済みにする
```

確認後の挙動:

| 現在状態 | 確認後の表示 | 状態変更 |
| --- | --- | --- |
| `注文前` | `建玉開始を確認しました。建玉状態を建玉中に変更できます。` | `建玉中に変更` ボタンを表示 |
| `建玉中` | `建玉開始の確認が完了しました。次は反対売買判断または満期管理です。` | 状態は変更しない |
| `決済済み` / `権利行使済み` / `満期終了` | `過去建玉の建玉開始情報を確認済みにしました。` | 状態は変更しない |

`建玉中に変更` ボタンを押した場合:

- `updateStatus("open")` または同等の既存状態変更処理を通す
- `1. 銘柄・価格` の建玉状態も `建玉中` に変わる
- ダッシュボードではメインの `現在管理中` に表示される
- `次にやること` は、約定確認済みなら `反対売買判断` へ変わる

7. 決済実績:

`7. 決済実績` は、Saxoで買戻しまたは売り決済が実際に約定した後の実績領域である。

決済実績が確認済みになった時点で、現在状態が `建玉中` なら次の案内を表示する。

```text
決済実績が入力されています。建玉状態を決済済みに変更できます。
```

ボタン:

```text
決済済みに変更
```

押下時:

- `updateStatus("closed")` または同等の既存状態変更処理を通す
- `1. 銘柄・価格` の建玉状態も `決済済み` に変わる
- ダッシュボードでは通常の現在管理中から外れ、履歴表示側へ移る
- 税務区分別履歴集計では `optionCloseExecutions` の実績値を使う

満期終了記録:

`7. 決済実績` に `closeKind = "expired"` の満期終了記録がある場合、現在状態が `建玉中` なら次の案内を表示する。

```text
満期終了の記録が入力されています。建玉状態を満期終了に変更できます。
```

ボタン:

```text
満期終了に変更
```

押下時:

- `updateStatus("expired")` または同等の既存状態変更処理を通す
- ダッシュボードでは履歴表示側へ移る
- 買戻し価格は不要

権利行使済み:

権利行使は、建玉開始確認や決済実績入力だけでは自動判定しない。利用者が状態ワークフローで `権利行使済み` を選ぶ。

選択時の誘導:

| 条件 | 誘導先 | 表示 |
| --- | --- | --- |
| P売りあり | 株式取得記録 | `P売りが権利行使された場合は、取得株数・取得単価・取得口座を記録してください。` |
| C売りあり、現物あり | 現物株の譲渡記録 | `C売りが権利行使された場合は、現物株の譲渡記録を入力してください。` |
| C売りあり、現物不足またはネイキッドCを含む | 決済実績と警告確認 | `現物が不足するC売りの権利行使は、通常の譲渡記録だけでは完結しません。Saxo上の処理と損益を確認してください。` |
| C脚とP脚が両方ある | 両方の候補を表示 | `C脚とP脚のどちらが権利行使されたかを確認し、必要な記録を入力してください。` |

ダッシュボード側の期待挙動:

- `注文前` と `建玉中` はメイン表示
- `決済済み` / `権利行使済み` / `満期終了` は履歴側へ移る
- `次にやること` は状態と実績入力状況から都度計算する
- `次にやること` ボタンを押すと、該当カードを開き、スクロールし、可能なら最初の未入力欄へフォーカスする
- 状態変更後は短い通知を表示し、利用者に「なぜ表示場所が変わったか」を伝える

状態変更後の通知例:

```text
建玉状態を決済済みに変更しました。この建玉は履歴に移動します。履歴を表示すると確認できます。
```

```text
建玉状態を満期終了に変更しました。この建玉は履歴に移動します。履歴を表示すると確認できます。
```

実装上の注意:

- `confirmOptionEntryExecutions` は `confirmed = true` の更新だけで終わらせず、現在状態に応じた状態変更案内を表示する
- `optionCloseExecutions` の結果が計算可能になった場合、状態変更案内を出す
- `closeKind = "expired"` の結果がある場合、`満期終了に変更` の案内を出す
- 状態変更ボタンは直接 `status` を書き換えず、既存の `updateStatus` と同等の遷移処理を使う
- 口座現金残高の反映は状態変更とは別処理とする。P口座では `記帳額 JPY` がある場合だけ、口座カードに `未反映の現金増減` と `現金残高に反映` を表示する

#### 8.6.2 決済実績記録

`反対売買判断` と `決済実績記録` は分ける。

`反対売買判断` は、Saxoの決済チケットに表示される現在価格を入れて、途中決済するかどうかを判断するための概算領域である。ここに入力した買戻し価格は、まだ実際の約定結果ではない。

`決済実績記録` は、Saxoで買戻し注文が実際に約定した後、Saxoの注文履歴詳細を見ながら入力する実績領域である。建玉状態を `決済済み` にする場合は、原則としてこの実績記録を入力する。

追加するデータ型:

```ts
type OptionCloseExecution = {
  id: string;
  legId: string;
  closeDate: string;
  contracts: number;
  closePriceUSD?: number;
  commissionUSD?: number;
  commissionJPY?: number;
  fxRateJPY?: number;
  settlementCurrency: "JPY" | "USD";
  brokerBookedAmountJPY?: number;
  brokerRealizedPnlJPY?: number;
  brokerTransactionCostJPY?: number;
  brokerPremiumJPY?: number;
  brokerFeeJPY?: number;
  brokerExchangeFeeJPY?: number;
  brokerExchangeConversionFeeJPY?: number;
  brokerFeeTaxJPY?: number;
  brokerExchangeRateJPY?: number;
  inputMode?: "P_JPY_BROKER_STATEMENT" | "USD_EXECUTION_CALC";
  source: "manual" | "broker_statement" | "saxo_api_estimate";
  confirmed?: boolean;
  memo?: string;
};

type TradeSimulation = {
  optionCloseExecutions?: OptionCloseExecution[];
};
```

入力項目の原則:

- 対象脚は通常カード見出しで表示し、単一脚では入力欄にしない
- 約定数量は対象脚の数量を初期値にし、部分決済の時だけ編集欄を開く
- 決済時刻、注文ID、ポジションID、取引番号は通常入力には出さず、必要時のメモに留める
- P口座 / DEMOではSaxoのJPY実績値を主入力にする
- N口座ではUSD実績値を主入力にする

決済実績の下書きと確認済み:

- `OptionCloseExecution` には `confirmed` を持たせる
- `反対売買判断` から `この価格で決済実績に反映` を押した場合は、決済実績カードへ値をコピーした下書きを作るだけで、`confirmed: false` とする
- 建玉状態を `建玉中` から `決済済み` へ変更して自動作成された下書きも、`confirmed: false` とする
- 利用者がSaxo注文履歴を見て、決済日、約定価格、P口座なら `Saxo実現損益 JPY`、N口座ならUSD実績を確認した後、`決済実績を確認済みにする` を押した時だけ `confirmed: true` とする
- `決済済みに変更` のワークフロータスクは、`confirmed: true` の買戻し決済実績がある時だけ表示する
- `confirmed: false` の下書きがある場合は、`決済実績を確認` と表示し、決済実績カードへ誘導する
- `calculateOptionCloseExecutionResults` は結果表示用に下書きから概算を計算してもよいが、ワークフロー上の「決済済みに変更可能」判定には使わない

決済実績カードは、P口座 / DEMO と N口座で入力欄を切り替える。共通UIとして `決済時手数料 USD` を常に主入力にしてはいけない。

注文ID、ポジションID、決済時刻は通常入力には出さない。利用者にとって日常的な損益管理、年率計算、税務区分集計には不要な識別情報であるため、必要な場合はメモ欄に任意で記録する程度にとどめる。

P口座 / DEMO_JPY_BASE の入力モード:

SaxoのP口座では、取引履歴に以下のJPY実績が表示される。P口座の決済実績入力では、この表示順に近い形で入力欄を並べる。

```text
銘柄
売買
新規/決済
価格
数量
取引日
記帳額 (JPY)
実現損益 (JPY)
取引費用 (JPY)

詳細:
手数料
為替変換手数料
取引所手数料
プレミアム
手数料・消費税額
為替レート
```

P口座での最小入力欄:

- 対象脚: 自動選択。通常はカード見出しに表示し、単一脚なら入力欄にしない
- 決済日 / 取引日: Saxo履歴の取引日
- 約定価格 USD: Saxo履歴の価格。反対売買判断に入力済みなら初期値にする
- Saxo実現損益 JPY: Saxo履歴の `実現損益 (JPY)`

P口座の照合用補助欄:

- 記帳額 JPY
- 取引費用 JPY
- 為替レート
- 約定数量
- メモ: 必要なら注文ID、ポジションID、取引番号などを任意で記録

補助欄は初期表示で大きく出さない。`照合用の詳細を開く` で表示する。

`照合用の詳細を開く` の中でも、最初から多数の費用内訳入力を並べない。利用者がSaxo履歴から通常確認したいのは、主に `記帳額 JPY`、`取引費用 JPY`、`為替レート` である。

手数料内訳は、さらに小さい `費用内訳を個別に記録する` を開いた場合だけ表示するか、メモ欄に残せるようにする。

費用内訳の任意項目:

- プレミアム JPY
- 手数料 JPY
- 為替変換手数料 JPY
- 取引所手数料 JPY
- 手数料・消費税額 JPY

これらは通常の損益計算には使わない。Saxoの表示を監査・照合したい場合だけ入力する。

P口座 / DEMO_JPY_BASE では、JPYの実績値を主入力にする。

- `実現損益 JPY` は主入力
- `取引費用 JPY` は照合用。`実現損益 JPY` 入力済みなら損益計算で追加控除しない
- `記帳額 JPY` は照合用。キャッシュフロー確認に使う
- `為替レート` は照合用。Saxo実現損益JPYがある場合は再計算に使わない
- `手数料 JPY`、`為替変換手数料 JPY`、`取引所手数料 JPY`、`手数料・消費税額 JPY` は詳細内訳として入力できる
- `決済時手数料 USD` は通常表示しない。SaxoのP口座履歴でUSD手数料が別途確認できる特殊ケースだけ、詳細欄に参考入力として置く
- `JPY手数料 任意` という表記は禁止する。P口座ではJPY費用が主であり、任意扱いに見せない

P口座の決済実績では、`Saxo実現損益 JPY` が入っていれば、それだけで税務区分別履歴、実現損益、決済実績年率を計算できるようにする。`取引費用 JPY` や詳細内訳は照合・メモ用途であり、`Saxo実現損益 JPY` に追加で差し引かない。

P口座の費用入力は、詳細内訳を全部入れなくてもよい。`取引費用 JPY` を入力した場合は照合用として表示し、詳細内訳を入力した場合は、`取引費用 JPY` と内訳合計が大きくずれていれば注意を出す。ただし、`実現損益 JPY` を上書き計算しない。

P口座では、`実現損益 JPY` が入力されている場合、その値を決済済みオプションの実現損益として優先する。これはSaxo側でJPY決済された実績値であり、アプリ側のUSD×為替による再計算より優先度が高い。

`実現損益 JPY` が未入力の場合は、可能であれば `記帳額 JPY` などから補助計算し、それもなければUSD約定価格と為替から概算する。ただし概算値であることを明示する。

P口座の最小入力例:

```text
決済日: 2026/06/02
約定価格 USD: 0.13
Saxo実現損益 JPY: 15,491
```

この3つがあれば、履歴、税務区分別集計、決済実績年率を計算できる。`記帳額 -2,461 JPY`、`取引費用 -384 JPY`、`為替レート 161.391940` は照合用詳細であり、通常入力では必須にしない。

P口座の画面例では、次のように解釈する。

```text
価格: 0.13               -> 約定価格 USD
数量: 1                  -> 自動数量。部分決済でなければ編集不要
取引日: 2026/06/02       -> 決済日
記帳額 (JPY): -2,461     -> 照合用の現金反映額
実現損益 (JPY): 15,491   -> 主計算に使う決済実績損益
取引費用 (JPY): -384     -> 照合用の費用。実現損益に追加控除しない
為替レート: 161.391940   -> 照合用。実現損益JPYがある時は再計算に使わない
```

したがって、通常入力の初期表示は `決済日`、`約定価格 USD`、`Saxo実現損益 JPY` の3項目に絞る。`記帳額 JPY`、`取引費用 JPY`、`為替レート`、詳細費用は `照合用の詳細を開く` の中に置く。

照合用詳細の表示階層:

```text
初期表示:
  - 決済日 / 取引日
  - 約定価格 USD
  - Saxo実現損益 JPY

照合用の詳細を開く:
  - 記帳額 JPY
  - 取引費用 JPY
  - 為替レート
  - 数量を変更（部分決済時だけ）
  - メモ

費用内訳を個別に記録する:
  - プレミアム JPY
  - 手数料 JPY
  - 為替変換手数料 JPY
  - 取引所手数料 JPY
  - 手数料・消費税額 JPY
```

費用内訳を個別に記録する領域は、初期状態では閉じる。入力しなくても、決済実績の履歴・税務集計・年率計算は成立する。

数量:

- 通常は対象脚の数量を自動使用する
- 部分決済の場合だけ `数量を変更` を開いて編集できるようにする
- 単一脚・全量決済では数量入力欄を常時表示しない

N口座 / PROD_N_USD_SETTLEMENT の入力モード:

N口座では、USDが主帳簿である。決済実績入力はUSDベースを主にし、JPYは参考換算に限定する。

N口座での推奨入力欄:

- 対象脚
- 取引日
- 約定価格 USD
- 数量
- USD手数料
- USD実現損益、またはアプリ計算のUSD実現損益
- 参考為替
- 参考JPY換算
- メモ: 必要なら注文ID、ポジションID、取引番号などを任意で記録

N口座では、P口座用の `記帳額 JPY`、`実現損益 JPY`、`取引費用 JPY` を主入力として扱わない。表示する場合も参考欄にとどめる。

N口座 / PROD_N_USD_SETTLEMENT では、USDの実績値を主入力にする。

- `USD手数料` は主入力
- `USDプレミアム` または `USD実現損益` は主入力
- `参考為替` は参考JPY換算のためだけに使う
- JPY費用欄を主入力として表示しない

Saxo注文履歴詳細との対応:

```text
買い決済 1プット @ 0.17
注文ID、ポジションID、受付/約定時間はSaxo上で確認できるが、アプリの通常入力項目にはしない
約定価格
約定数量
取引手数料
```

注意:

- Saxoの注文履歴詳細のタイトルに出る `買い決済 1プット @ 0.17` は注文価格であり、実際の約定価格とは異なる場合がある
- 実績入力で使う価格は、明細行の `約定` 行に表示される価格を優先する
- `注文済み` 行の価格は、発注時の指値としてメモ扱いにする
- P口座の取引履歴一覧に `実現損益 (JPY)` が表示されている場合は、その値を最優先で入力・集計する

計算:

共通のUSD計算は、Saxo実績JPYが取れない場合の概算またはN口座用である。P口座で `brokerRealizedPnlJPY` が入力されている場合は、以下のUSD再計算を主計算に使わない。

```text
建て時プレミアム = 建てプレミアムUSD × 100 × 枚数
買戻しコスト = 約定買戻し価格USD × 100 × 約定枚数
手数料 = 建て時手数料 + 決済時手数料

実現損益 = 建て時プレミアム - 買戻しコスト - 手数料
```

P口座、DEMO / JPYベース:

```text
実現損益JPY =
  (建て時プレミアムUSD - 買戻しコストUSD - 手数料USD)
  × 適用為替
  - JPY手数料
```

ただし、P口座のSaxo取引履歴で `実現損益 (JPY)` が確認でき、利用者が `brokerRealizedPnlJPY` に入力した場合は、その値を最優先する。

優先順位:

1. `brokerRealizedPnlJPY`: Saxo取引履歴の `実現損益 (JPY)`
2. `brokerBookedAmountJPY` など明細JPY値からの補助計算
3. USD約定価格、USD手数料、為替からのアプリ概算

1または2を使った場合は `Saxo実績ベース` と表示する。3を使った場合は `概算` と表示する。

`brokerRealizedPnlJPY` を使う場合、`brokerTransactionCostJPY`、`brokerFeeJPY`、`brokerExchangeFeeJPY` などをさらに控除しない。Saxoの実現損益JPYは、P口座の履歴上で確定表示された実績値として扱う。費用欄は照合・メモ用途である。

決済実績カードの計算内訳表示:

P口座 / DEMO_JPY_BASE で `brokerRealizedPnlJPY` が入力されている場合、緑色の結果欄の `計算内訳` にUSD式を表示しない。

表示例:

```text
計算内訳
Saxo実績ベース / 実現損益 15,491 JPYを採用
参考: 記帳額 -2,461 JPY / 取引費用 -384 JPY / 為替 161.391940
```

この場合、`プレミアム $116.00 - 買戻し $13.00 - 手数料 $5.63` のようなUSD式は出さない。P口座ではSaxoがJPYで確定表示した実現損益を採用しているため、USD式は概算説明としても前面に出すべきではない。

P口座で `brokerRealizedPnlJPY` が未入力で、USD約定価格・為替から概算している場合だけ、次のように明示する。

```text
計算内訳
概算 / USD価格と為替から参考計算。Saxo実現損益JPYを入力すると実績ベースになります。
```

N口座 / PROD_N_USD_SETTLEMENT の場合はUSDが主帳簿なので、USD式を表示してよい。

表示例:

```text
計算内訳
USD実績ベース / プレミアム $116.00 - 買戻し $13.00 - 手数料 $4.50
参考JPY換算: 18,xxx JPY
```

N口座:

```text
実現損益USD =
  建て時プレミアムUSD
  - 買戻しコストUSD
  - USD手数料

JPYは参考換算だけにする。
```

税務集計:

- `closed` かつ `optionCloseExecutions` あり: 決済実績から先物取引に係る雑所得等の実現損益を計算する
- `closed` だが `optionCloseExecutions` なし: 税務区分別履歴集計では `実績未入力` として注意表示する。推定値で確定集計しない
- `expired`: 権利行使されず満期終了した売り建ては、プレミアムから手数料を引いた金額を実現損益として扱う
- `assigned`: オプション損益はプレミアムから手数料を引いた金額。株式取得または譲渡は別カードで扱う

年率:

```text
決済実績年率 =
  決済実績損益
  ÷ 使用分母
  × (365 ÷ 建玉日から実際の決済日までの日数)
  × 100
```

- 決済日が入力されている場合は、反対売買判断日の代わりに実際の決済日を使う
- 決済実績年率は、履歴側のオプション実現損益にも表示する

当年成績サマリー:

履歴が増えると、ダッシュボード上の個別建玉だけでは「今年どれだけ利益または損失が出たか」が分かりにくくなる。そのため、結果状態になった履歴から当年の実現損益を集計する `当年成績サマリー` を追加する。

表示位置と導線:

- 当年成績サマリーは、建玉管理画面の途中に常時表示しない
- アプリ上部のヘッダーに `成績` ボタンまたはタブを追加し、クリックすると独立した `成績画面` に切り替える
- 初期表示は従来どおり `建玉管理` 画面にする。建玉管理画面は、建玉ダッシュボード、口座余力、選択建玉の詳細確認に集中させる
- `成績画面` は、当年実現損益、月別チャート、税区分別内訳、銘柄別集計、未確認履歴をまとめて確認する画面とする
- ヘッダーの `成績` ボタンは小さく、他の操作ボタンと同じ高さに揃える。必要なら `BarChart3` などのチャート系アイコンを使う
- `建玉管理` 画面には、成績カードを常設しない。成績確認が必要な時だけ、ヘッダーの `成績` から開く
- 成績画面内には `建玉管理へ戻る` 導線を置く

対象年:

- 初期表示はブラウザローカル日付の当年
- 年セレクタで過去年を切り替えられる
- 対象日は、オプション決済実績は `closeDate`、満期終了は満期終了記録日または満期日、株式譲渡は譲渡日を使う

集計対象:

- `決済済み`、`権利行使済み`、`満期終了` など結果状態の履歴だけを対象にする
- `建玉中`、`注文前`、反対売買判断の見積もり、未確認の決済実績下書きは対象外
- オプション決済は `OptionCloseExecution.confirmed === true` の実績だけを集計する
- P口座 / DEMO_JPY_BASE では `Saxo実現損益 JPY` を最優先する
- N口座ではUSD実現損益を主集計にし、JPYは参考換算として別表示する。P口座JPY損益とN口座USD損益を無理に1つの確定JPY損益へ混ぜない
- 株式譲渡損益は、現物株の譲渡記録がONで入力済みのものだけを `上場株式等の譲渡所得等` として集計する
- 未売却株の含み損益、満期ペイオフの想定損益、カバードコール満期想定損益は当年実現損益に混ぜない

成績画面の上部サマリー:

```text
2026年 実現損益
合計 +15,491円
オプション +15,491円 / 株式 0円
未確認 0件
[詳細]
```

N口座USDが含まれる場合:

```text
2026年 実現損益
P口座JPY +15,491円
N口座USD +$94.50（参考 +15,300円）
未確認 0件
```

成績画面の詳細表示:

- 月別実現損益チャート
  - 横軸: 1月〜12月
  - 縦軸: 実現損益
  - 棒グラフ: 月ごとの実現損益
  - 折れ線: 年初来累計
  - 利益は緑、損失は赤または控えめな赤系
- 税区分別サマリー
  - `先物取引に係る雑所得等`: オプション実現損益
  - `上場株式等の譲渡所得等`: 株式譲渡損益
  - `配当所得・外国税額控除`: 未集計または将来対応
  - N口座USDは別枠で表示し、参考JPY換算であることを明示
- 銘柄別サマリー
  - 銘柄ごとの実現損益
  - 件数
  - 勝ち負けではなく、実現損益の合計を主にする
- 未確認・未入力リスト
  - 決済済みだが決済実績未確認
  - 権利行使済みだが株式取得または譲渡記録未入力
  - 満期終了だが満期終了記録未確認
  - クリックで該当カードへ移動する

UI上の注意:

- このカードは「当年の確定済み履歴の集計」であり、現在建玉の含み損益や将来想定は含まない
- 税務区分は概算整理であり、確定申告用の税額ではない
- N口座のJPY換算は参考値であり、円転または税務上の確定損益ではない

UI:

`手入力ウィザード` に `7. 決済実績` カードを追加する。

表示条件:

- 建玉状態が `決済済み`
- または `反対売買判断` から `この価格で決済実績に反映` を押した場合

反対売買判断カードには、脚ごとに次のボタンを置く。

```text
この価格で決済実績に反映
```

クリック時:

1. 現在の買戻し価格を `closePriceUSD` にコピーする
2. 決済日を今日にする
3. 約定数量を対象脚の数量にする
4. 決済時手数料USDの初期値を `2.25` にする
5. `7. 決済実績` カードを開く
6. 利用者がSaxo注文履歴詳細を見ながら約定価格、数量、実際の手数料を修正できるようにする。IDや時刻が必要な場合はメモ欄に任意で記録する

建玉状態を `決済済み` に変更した時の自動導線:

- 利用者が建玉状態を `建玉中` から `決済済み` に変更した時点で、売り建てオプション脚ごとの `OptionCloseExecution` 下書きを自動作成する
- 既に対象脚の `OptionCloseExecution` がある場合は重複作成しない
- 下書きの初期値は以下とする
  - `closeDate`: 今日
  - `contracts`: 対象脚の数量
  - `closePriceUSD`: 反対売買判断に入力済みの買戻し価格があればその値、なければ空欄
  - `commissionUSD`: `2.25`
  - `settlementCurrency`: 口座分類に応じて `JPY` または `USD`
  - `source`: `manual`
- 下書き作成後、`7. 決済実績` カードを自動で開き、画面をスクロールする
- 可能であれば最初の未入力項目、通常は `約定価格 USD` にフォーカスする
- 自動作成された下書きは `未確定 / 入力中` の扱いにし、約定価格、数量、実際の手数料をSaxo注文履歴で確認してから保存・確定する導線にする。IDや時刻は必須にしない
- 利用者が誤って `決済済み` に変更した場合に備え、建玉状態を `建玉中` へ戻しても決済実績の下書きは自動削除しない。削除は利用者操作にする
- `決済済み` だが決済実績下書きが未完成の場合は、税務履歴・実現損益の確定集計には入れず、未入力警告を出す

建玉状態変更時の遷移先ルール:

`建玉状態` を `建玉中` から結果状態へ変更した場合は、戦略とオプション脚に応じて、利用者が次に入力すべきカードへ自動で移動する。

| 変更後の状態 | 対象脚 / 戦略 | 自動で開くカード | 目的 |
| --- | --- | --- | --- |
| `決済済み` | P売り / C売り / 複合戦略の各売り脚 | `7. 決済実績` | Saxo注文履歴の買戻し約定価格、約定日、手数料を記録する |
| `満期終了` | 権利行使されず失効したP売り / C売り | `7. 決済実績` の満期終了モード | 買戻しなしでプレミアムが確定した履歴を残す |
| `権利行使済み` | P売り | `5. 現物株の取得記録` または株式取得カード | P権利行使により株を買い受けた記録を作る |
| `権利行使済み` | カバードコール / C売り | `5. 現物株の譲渡記録` | C権利行使により株を渡した、または売却扱いになった記録を作る |
| `権利行使済み` | ネイキッドコールを含むC売り | `7. 決済実績` とリスク警告 | 現物がないため通常の譲渡記録だけでは完結しない。Saxo上の処理確認を促す |
| `権利行使済み` | カバードコール＋追加P売り | C脚は `現物株の譲渡記録`、P脚は必要に応じて `現物株の取得記録` | 複数脚が同時に結果状態を持つ可能性があるため、脚ごとに記録先を分ける |

`満期終了` の扱い:

- 満期終了は買戻し約定ではないため、`closePriceUSD` は `0` ではなく「買戻しなし」として扱う
- 実現損益は、受取プレミアムから建て時手数料を引いた金額とする
- 満期終了日を実現日として、履歴・税務区分別集計へ反映する
- UI上は `7. 決済実績` カード内に `満期終了モード` を用意し、約定価格欄を表示しない、または無効化する
- `満期終了` に変更したら、満期終了記録の実現日は本日を初期表示し、カードへスクロールする。契約上の満期日は変更しない。実際の満期日で記録したい場合は利用者が実現日を修正する

`権利行使済み` の扱い:

- P売りの権利行使は、株式を買い受けるイベントである。`現物株の取得記録` に、取得日、取得株数、取得単価、手数料、口座区分を入力させる
- C売りの権利行使は、保有株を渡す、または売却扱いになるイベントである。`現物株の譲渡記録` に、譲渡日、譲渡株数、譲渡単価、取得単価、手数料、口座区分を入力させる
- オプション側のプレミアム損益は `先物取引に係る雑所得等`、株式取得・譲渡側は `上場株式等の譲渡所得等` として分ける
- `権利行使済み` に変更しただけで株式損益を自動確定しない。利用者が株式取得または譲渡の実績入力を完了してから集計する
- 複合戦略では、P脚とC脚を個別に判定し、必要な記録カードを複数表示できるようにする

カード移動の原則:

- 状態変更後は、最も入力漏れリスクが高いカードを開いてスクロールする
- 既に該当する実績下書きがある場合は重複作成しない
- 誤操作で状態を戻しても、作成済みの実績下書きは自動削除しない
- 下書きが未完成の間は、履歴・税務・年率の確定集計には入れず、未入力警告を出す

警告:

```text
決済済みですが、決済実績が未入力です。Saxo注文履歴から約定価格と手数料を入力してください。
```

この警告は次の場合に出す。

- `simulation.status === "closed"`
- 対象の売り建てオプション脚がある
- `optionCloseExecutions` が空、または対象脚の実績が不足している

この警告には `決済実績を入力` のアクションを付け、クリックで `7. 決済実績` カードへスクロールする。

保存・復元:

- JSONバックアップに `optionCloseExecutions` を含める
- 旧JSONでは `optionCloseExecutions` がなくても壊れない
- 旧データの `closeCostUSD` は判断用価格として残し、勝手に実績へ変換しない

### 8.7 ホイール戦略カード

画面下部にコンパクト表示する。

ホイール戦略カードは常時大きく表示しない。銘柄ごとのホイールサイクルが存在しない場合、または選択中の建玉がまだホイール管理に登録されていない場合は、折りたたみ行または小さな導線だけを表示する。

表示ルール:

- ホイールサイクルが0件: `ホイール管理 0件` と小さく表示し、必要な時だけ開く
- P口座プット売り中: `ホイール準備中（P口座）` として表示し、N口座ホイール本体と区別する
- P→N移管後またはN口座運用中: `N口座ホイール管理` として表示する
- ダッシュボードやサマリー確認の邪魔にならないよう、初期状態は折りたたむ
- ユーザーが `選択建玉からサイクル作成` を押した場合のみ、その建玉をホイール管理へ登録する
- P口座プット売りを登録する場合は、N口座現金待機を通過済みにしない

通常:

```text
ホイール戦略
NVDA | Nカバードコール中 | +$612 | 100株
AMZN | Nプット売り中     | +$180 | 0株
```

展開時:

- 色付きフェーズステッパー
- 累積損益
- 保有株数と平均取得単価
- イベント履歴
- 関連建玉
- 次の候補アクション

## 9. 計算ロジック

### 9.1 P口座プレミアム

実明細がある場合:

```text
netCashflowJPY = brokerNetCashflowJPY
```

推定の場合:

```text
grossPremiumUSD = premiumUSD * 100 * quantity
grossPremiumJPY = grossPremiumUSD * appliedFxRate
netCashflowJPY = grossPremiumJPY - commissionJPY - exchangeFeeJPY
```

### 9.2 N口座プレミアム

```text
grossPremiumUSD = premiumUSD * 100 * quantity
netCashflowUSD = grossPremiumUSD - commissionUSD - exchangeFeeUSD
referenceJPY = netCashflowUSD * referenceFxRateJPY
```

年率計算はUSD同士で行う。

```text
annualReturnPct = profitUSD / denominatorUSD * (365 / days) * 100
```

JPY参考換算は年率の主計算には使わない。

### 9.3 N口座ホイール累積損益

```text
cumulativePremiumUSD
  + cumulativeStockRealizedPnlUSD
  - cumulativeFeesUSD
  = cumulativeTotalPnlUSD
```

未売却の株式含み損益は、実現損益とは別に表示する。

### 9.4 カバードコール満期想定損益

カバードコールでは、権利行使された場合の株式売却損益とオプションプレミアムを並べて表示する。

```text
株式売却損益 = (C権利行使価格 - 取得単価) * 株数
オプション損益 = Cプレミアム - 手数料
合計想定損益 = 株式売却損益 + オプション損益
```

N口座ではUSD主表示、JPYは参考表示。

### 9.4.1 ショートストラングルとネイキッドコールの満期シナリオ

ショートストラングル、または現物株なしのC売りを含む建玉では、カバードコールと同じシナリオ文言を使わない。

ショートストラングルの表示:

```text
レンジ内:
P売りとC売りの両方が権利行使されず、プレミアム獲得を想定

下抜け:
P売りの権利行使で株を買い受ける可能性、またはPを買い戻して閉じる判断
P側は、下落時に買い受ける資金と方針を確認する

上抜け:
C売りの買戻し、または権利行使リスクを確認する
現物なしのC売りでは、売却する現物株がないため、上抜け時の買戻しルールと許容損失額を確認する
```

資金表示:

- P売り側: `下落時に買い受ける資金`
- C売り側: `上抜け時に買い戻す資金・許容損失額`

現物なしのC売りでは、上方向の最大損失を固定値として断定しない。逆指値ラインがある場合も、ギャップや流動性不足により想定より不利な価格で約定する可能性があるため、警告として表示する。

### 9.5 税務

オプション損益と株式譲渡損益は別区分で表示する。

N口座でUSD運用中の損益は、JPY換算を確定値として扱わない。税務上の厳密な円換算は後続フェーズ。

## 10. リスク警告

### 10.0 警告レベルと集計ルール

警告は、利用者が「本当に注文前に止めるべきもの」と「確認情報」を混同しないように分類する。

```ts
type RiskSeverity = "info" | "warning" | "danger";
```

表示上の意味:

```text
info:
  情報表示。警告件数・注意件数には含めない。
  例: N口座のJPY換算は参考表示。

warning:
  注意。注文前NGではないが、確認した方がよい。
  ダッシュボードでは「注意n件」に含める。

danger:
  重大警告。blocking=true の場合は「注文前NG」として扱う。
```

ダッシュボードの集計:

```text
info 件数: 警告列の件数に含めない
warning 件数: 注意n件
danger かつ blocking=true: NGn件
danger かつ blocking=false: 注意n件、または重大注意として表示
```

`N口座のJPY換算は参考表示です` のような常時表示の注意喚起は、リスク警告カードではなく、N口座カードやヘルプ内の情報表示として扱う。どうしてもリスク警告一覧に出す場合も `info` とし、ダッシュボードの警告件数には含めない。

追加する警告:

1. P/N余力の混同
   - P口座のJPY残高をN口座建玉の余力に使っている可能性
   - N口座のUSD残高をP口座建玉の余力に使っている可能性

2. P口座に取得株が残っている
   - P口座で権利行使により株式取得済み
   - N口座へ移管していない

3. N口座カバードコールの株数不足
   - C売り数量 * 100 > N口座保有株数

4. 同一口座内でP売りとカバードコールを同時進行
   - 資料上は、通常ホイールはN口座中心、P口座は入口・資金待機として分ける前提
   - 必ずNGではないが、注意表示する

5. N口座JPY換算の誤解
   - 参考換算を確定損益と誤認しないよう表示
   - `info` として扱い、警告件数・注意件数には含めない

6. 最終円転未記録
   - N口座からJPY出金したが、出金・円転イベントが未入力

7. 買いたくないプット売りの出口未設定
   - `P売りの方針 = 株を取得したくない` の場合、利確ルール、損切りルール、満期前タイムリミット、Saxo決済注文方針のいずれかが未設定なら注意表示する
   - すべて未設定の場合は注文前NG候補とする

8. 満期前タイムリミット超過
   - 利用者が明示的に設定した `latestCloseDaysBeforeExpiry` を過ぎているのに建玉が未決済の場合、反対売買判断を促す
   - デフォルト値として自動で入っているだけの期限では、期限超過警告を出さない
   - 買いたくないプット売りでは強めの警告にする
   - C売りで `株を残したい` 場合も、判断期限超過時にC買戻し判断を促す
   - 警告には `反対売買判断へ` のリンクまたはボタンを付け、該当脚の反対売買判断カードへスクロールする

9. ネイキッドコールまたは未カバーC売り
   - C売り数量 * 100 が保有株数を上回る場合、未カバー部分をネイキッドコールとして扱う
   - `現物なし・上抜け時は買戻し` の方針、逆指値ライン、買戻し価格ライン、許容損失額のいずれも未設定なら注意または注文前NG候補にする
   - 逆指値ありでも、急騰、時間外の値飛び、寄付きギャップ、流動性不足で想定より不利に約定する可能性を警告する
   - `逆指値で損失限定` とは表示しない
   - `未カバーC売りがある`、`上抜け時のルールが未設定`、`期限到達` を別々に重複表示しすぎない
   - 推奨は、一覧上は1件の主要警告にまとめ、詳細内で不足項目を列挙する方式

10. 為替レート未入力
   - P口座、DEMO / JPYベースでは、JPY計算に必須のため `danger` とする
   - N口座ではUSD主計算は可能だが、JPY参考表示と税務参考値が崩れるため、文言を分ける
   - N口座向け文言では「すべて0円になります」と断定しない

警告表示位置:

- 建玉ダッシュボードの警告列
- リスク警告カード
- 必要に応じて出口ルールカード内の該当脚

警告クリック時の動作:

```text
ダッシュボード警告またはリスク警告をクリック
→ 対象建玉を選択
→ 反対売買判断カードを開く
→ 対象脚の入力欄へスクロール
```

警告文例:

```text
満期前の判断期限に到達しています。C買戻し価格を確認してください。
```

```text
満期前の判断期限に到達しています。P買戻し価格を確認してください。
```

### 10.1 現行警告文の修正方針

現行実装で修正が必要な警告:

1. `N口座のJPY換算は参考表示です`
   - 内容は妥当
   - `info` として扱い、ダッシュボードの `注意n件` には含めない

2. `裸コール部分があります` / `裸コールの上方向ルールが未設定です` / `未カバーC売りの出口ルールが未設定です`
   - 方向性は妥当
   - ただし重複して見えやすいため、利用者向けには `未カバーC売りの確認が必要です` を主警告にし、不足項目を本文で列挙する
   - `裸コール` より `未カバーC売り` を基本文言にする

3. `N口座のUSD現金残高が不足している可能性があります`
   - 内容は妥当
   - 判定はUSD残高とUSD買付資金で行う
   - JPY換算値で判定しない

4. `許容損失額`
   - P口座、DEMOはJPY
   - N口座はUSD
   - N口座でJPYの許容損失額をUSD損益と比較しない

5. `満期前判断期限に到達しています`
   - 利用者が明示設定した期限だけ警告対象にする
   - `株を取得してもよい` P売りでは、デフォルト期限だけで警告を出さない
   - `株を取得したくない` P売り、`株を残したい` C売り、`現物なし・上抜け時は買戻し` C売りでは、明示設定済みなら警告対象にする

6. `買いたくないP売りとしては権利行使価格が近い可能性があります`
   - 内容は妥当
   - 判定根拠を文言に入れる
   - 例: `現在株価に対してP権利行使価格が近い水準です。距離、損切り、分母を確認してください。`

7. `証拠金使用率が高いです`
   - 内容は妥当
   - 閾値を文言に入れる
   - 例: `証拠金使用率が60%以上です。建玉追加や急変時の余力を確認してください。`

8. `為替レートが未入力です`
   - P口座、DEMOでは現行方針でよい
   - N口座では `N口座はUSD主計算ですが、JPY参考表示と税務参考値に使うUSD/JPYが未入力です。` のように分ける

## 11. JSONバックアップ互換

スキーマを更新する。

```json
{
  "schemaVersion": 2,
  "app": "us-options-position-manager",
  "workspace": "live",
  "simulations": [],
  "accountStates": [],
  "wheelCycles": [],
  "wheelEvents": [],
  "fxTransfers": [],
  "stockTransfers": [],
  "exitOrderPlans": []
}
```

既存 `schemaVersion: 1` は、読み込み時に次の既定で移行する。

- `accountCode = "P"`
- `accountCurrency = "JPY"`
- 既存JPY計算を維持
- 旧 `marginRequirement` がある場合は `marginAvailable` へ移行する
- ホイールイベントなし

## 12. 実装工程

### Phase 0: 保全と仕様固定

目的: 現行ローカル版を壊さない。

作業:

- `http://127.0.0.1:5173/` の現行データをJSONバックアップ
- 現行コードのブランチまたはタグ固定
- `schemaVersion: 1` データの移行ルールをテスト化

完了条件:

- 既存データを消さずにアプリを起動できる
- 旧JSONを読み込める

### Phase 1: P/N口座基盤

目的: 口座と通貨の前提を正す。

作業:

- `SaxoAccountCode` 型追加
- `AccountState` をP/N別に変更
- `AccountState.marginRequirement` を `marginAvailable` へ移行
- 口座全体カードの入力ラベルを `必要証拠金` ではなく `必要証拠金余力` に変更
- 建玉に `accountCode` を追加
- ダッシュボードに口座列追加
- 口座全体カードをP/N別表示に変更
- P/N合算警告を除去

完了条件:

- P口座建玉とN口座建玉を同時に保存できる
- P/Nで現金残高と証拠金使用率が分かれる
- Saxo画面下部の `必要証拠金余力` をそのまま入力できる
- 旧JSONの `marginRequirement` が読み込み時に `marginAvailable` として引き継がれる

### Phase 2: P/N別キャッシュフロー計算

目的: JPY口座とUSD口座の計算を分離する。

作業:

- P口座用JPY計算
- N口座用USD計算
- N口座参考JPY表示
- 分母比較をP/N通貨別に修正
- 年率計算の根拠表示も通貨別に修正

完了条件:

- N口座のUSD/JPY更新でUSD年率が変わらない
- P口座はJPYベースで従来通り計算できる

### Phase 3: 株式移管イベント

目的: P口座取得株をN口座ホイールへ渡せるようにする。

作業:

- `StockTransferEvent` 型追加
- P→N株式移管入力UI
- 移管後にN口座保有株へ反映
- P口座取得株残り警告
- JSONバックアップ対応

完了条件:

- P口座で取得した100株をN口座へ移管記録できる
- 移管後、N口座カバードコールの保有株として使える

### Phase 4: N口座ホイール管理

目的: 銘柄ごとのUSD建てホイール台帳を作る。

作業:

- `WheelCycle` v2型
- `WheelEvent` 型
- 銘柄別ホイールカード
- 色付きフェーズステッパー
- イベント履歴
- 累積プレミアムUSD
- 累積株式売買損益USD
- 累積手数料USD
- 参考JPY換算
- 次の候補アクション

完了条件:

- NVDAなど1銘柄で、P売り、株取得、P→N移管、Nカバードコールまで1サイクルで見える
- N口座内ホイールはUSDで累積される
- P口座でプット売り中の建玉をホイールへ登録しても、N口座現金待機が「済」にならない
- N口座に0 USDしかない状態で、N口座プット売りが「現在」と表示されない
- P口座段階は `ホイール準備中（P口座）` または同等の表現で、N口座ホイール本体と区別される
- ホイールカードは初期表示で画面を大きく占有せず、必要な時だけ展開できる

### Phase 5: リスク警告とシナリオ再調整

目的: P/Nとホイールに合わせて文言と警告を整える。

作業:

- 警告レベルを `info` / `warning` / `danger` に整理
- `info` はダッシュボードの警告件数・注意件数に含めない
- `N口座参考JPY注意文言` は情報表示として扱い、注意件数に混ぜない
- N口座の許容損失額はUSD主入力にし、JPY値をUSD判定へ流用しない
- 満期前タイムリミット警告は、利用者が明示設定した期限だけを対象にする
- ネイキッドコール警告を `未カバーC売り` 文言へ統一し、重複警告を整理する
- 為替未入力警告はP/DEMOとN口座で文言を分ける
- `買いたくないP売りとして権利行使価格が近い` 警告に判定根拠を加える
- `証拠金使用率が高い` 警告に60%以上などの閾値を加える
- N口座カバードコール株数不足警告
- P口座株式残り警告
- P/N余力混同警告
- N口座参考JPY注意文言
- プット売り、カバードコール、ホイールのシナリオ文言再整理
- ネイキッドコールを含むショートストラングルの上方向シナリオ追加
- C売りがカバー済みか未カバーかを保有株数から判定する

完了条件:

- ユーザーが「次に何を確認すべきか」を口座別に理解できる
- 確定損益、含み損益、参考JPY換算が混同されない
- カバードコールとネイキッドコールの文言・警告・出口判断が混同されない
- 情報表示、注意、注文前NGがダッシュボード上で混同されない
- N口座でUSDとJPYの許容損失額が混同されない
- デフォルト値だけで不要な期限警告が出ない

### Phase 5.5: 出口注文・IFD/OCO設計

目的: 買いたくない・買えないプット売りや、途中決済前提の売り建てオプションで、利確・損切り・満期前決済ルールを注文前に決められるようにする。

作業:

- `ExitOrderPlan` 型または既存 `ProfitTakeRule` / `StopLossRule` の拡張
- `4. ルール・表示設定` を `4. 出口ルール・表示設定` へ改名
- `Saxoで決済注文を設定する` と `建て注文に出口を添付する` の2チェックを廃止し、1つの選択 `Saxo出口注文の使い方` に統合
  - 使わない（手動判断）
  - 建玉後に決済注文を入れる
  - 新規注文と同時にIFD/OCOを入れる
- Saxo注文タイプ入力
  - なし
  - 決済指値
  - 決済逆指値
  - OCO
  - IFD
  - IFD-OCO
- 利確確保率から利確買戻し価格を自動計算
- 損切りルールの入力整理
- 満期前タイムリミット入力
- Saxo注文タイプに応じた利確/損切り欄の自動表示
  - 決済指値: 利確のみ
  - 決済逆指値: 損切りのみ
  - OCO / IFD-OCO: 利確と損切りの両方
  - 手動判断: アプリ内判断基準として任意表示
- 売り脚ごとの出口ルール適用
  - P売りの出口ルールをC売りへ無条件に流用しない
  - C売りで株を売却されてもよい場合は、C買戻し判断を通常非表示または任意表示にする
  - C売りで株を残したい場合だけ、C脚専用の買戻し判断を表示する
  - C売りで現物なし・上抜け時は買戻しの場合は、ネイキッドコール用の逆指値ライン、買戻し価格ライン、許容損失額を表示する
- `主分母` と `NISA等 比較年率` を出口ルールカードから分離し、`表示・比較設定` カードへ移動
- 満期前タイムリミット超過警告
- 反対売買判断カードとの同期
- 買いたくないプット売りで出口未設定の場合の注意/NG
- JSONバックアップ/復元対応
- 既存データ移行

完了条件:

- 買いたくないプット売りで、利確・損切り・満期前決済ルールを注文前に記録できる
- 受取プレミアムと確保率から、利確買戻し価格が表示される
- 反対売買判断に現在買戻し価格を入れると、利確ライン到達/未到達、損切りライン到達/未到達が分かる
- 反対売買判断に現在買戻し価格を入れると、今閉じた場合の概算損益と途中決済年率が分かる
- 途中決済年率は、満期日までの日数ではなく、建玉日から反対売買判断日までの実経過日数で計算される
- 満期前タイムリミットまで残り何日か分かる
- Saxoで設定する決済指値・逆指値の目安が分かる
- 決済指値では利確ルールだけ、決済逆指値では損切りルールだけ、OCO/IFD-OCOでは両方が表示される
- カバードコール＋追加P売りで、P売りの利確/損切り/満期前タイムリミットがC売りへ同じ条件で表示されない
- C売りで株を売却されてもよい場合、反対売買判断のCカードは通常不要であることが分かる
- C売りで株を残したい場合だけ、C買戻し価格による判断ができる
- C売りで株を残したい場合、出口ルールカードは単なるメモではなく、C買戻し判断への導線・期限警告として機能する
- C売りで現物なし・上抜け時は買戻しの場合、ネイキッドコールとして上方向リスク、買戻しルール、許容損失額、ギャップ警告が表示される
- 主分母とNISA等比較年率が出口ルールカードから分離されている
- 満期前タイムリミットに到達した建玉には警告が出る
- 満期前タイムリミット警告から、該当する反対売買判断カードへクリックで移動できる
- `株を取得してもよい` プット売りでは出口未設定を強制NGにしない
- `株を取得したくない` プット売りでは出口未設定を注意または注文前NG候補にする
- アプリから発注はしない
- 既存建玉と旧JSONが壊れない

### Phase 6: 税務・履歴集計の拡張

目的: P/NとUSD運用を踏まえて税務表示を破綻させない。

作業:

- `OptionEntryExecution` 型を追加する
- `TradeSimulation.optionEntryExecutions` を追加する
- `WorkflowTask` / `WorkflowTaskType` の派生型または同等のUIタスク型を追加する
- domain層に `getWorkflowTasks(simulation)` を追加する
- 建玉状態を `注文前` から `建玉中` に変更した時、売り建てオプション脚ごとの建玉約定確認下書きを自動作成する
- 建玉状態を変更した直後に、状態別のガイド通知を表示する
- 建玉状態変更UIを、フォーム内の単なるプルダウンだけでなく、編集画面上部の状態ステッパーまたは状態変更ボタンとして目立たせる
- 建玉約定確認カードを追加し、P口座 / DEMOではSaxo取引履歴の `記帳額 (JPY)`、`プレミアム (JPY)`、`取引費用 (JPY)`、`為替レート` を入力しやすくする
- P口座 / DEMOの建玉約定確認では、主入力を `記帳額 JPY`、`プレミアム JPY`、`取引費用 JPY` に絞り、為替レートと詳細内訳は折りたたみ補助欄にする
- P口座 / DEMOの建玉約定確認では、`取引手数料 USD` を通常表示しない
- P口座 / DEMOでは、`記帳額 JPY` を建玉時の現金反映額として最優先する
- P口座 / DEMOでは、`プレミアム JPY - abs(取引費用 JPY)` を記帳額として扱わない
- P口座 / DEMOでは、`プレミアム JPY` と `記帳額 JPY` が入力された場合、差額を参考表示する
- N口座では建玉約定確認をUSD主入力にし、JPYは参考換算に限定する
- 建玉中だが約定情報未確認の場合は、`約定情報未確認` を表示し、クリックで建玉約定確認カードへ移動する
- 建玉ダッシュボードに `次にやること` 列を追加し、最優先 `WorkflowTask` を表示する
- `次にやること` ボタンをクリックしたら該当カードを開き、スクロールし、可能なら未入力欄へフォーカスする
- ナビゲーション先カードを3〜5秒だけ淡色枠でハイライトする
- `3-A. 建玉約定確認` を `3-A. 建玉開始の約定確認` に改名する
- `約定情報を確認済みにする` ボタンを `建玉開始を確認済みにする` に改名する
- 建玉開始確認後、現在状態が `注文前` の場合は `建玉中に変更` ボタンを表示し、押下時に既存の状態変更処理で `建玉中` へ遷移する
- 建玉開始確認後、現在状態が `建玉中` の場合は状態を変えず、次アクションが `反対売買判断` または満期管理へ進むようにする
- `OptionCloseExecution` 型を追加する
- `TradeSimulation.optionCloseExecutions` を追加する
- 反対売買判断の買戻し価格を、実績決済として確定保存できる導線を追加する
- 手入力ウィザードに `7. 決済実績` カードを追加する
- P口座 / DEMO_JPY_BASE の決済実績では、初期表示を `取引日`、`約定価格 USD`、`Saxo実現損益 JPY` に絞る。対象脚と数量は自動表示にし、部分決済時だけ数量編集を開く
- P口座 / DEMO_JPY_BASE の `記帳額(JPY)`、`取引費用(JPY)`、`為替レート` は `照合用の詳細を開く` の中に移す。費用内訳はさらに `費用内訳を個別に記録する` の二段目、またはメモ欄に移す
- P口座では、Saxo取引履歴の `実現損益 (JPY)` が入力されている場合、その値を決済済みオプションの実現損益として最優先する
- P口座では、`決済時手数料 USD` を主入力として表示しない。`JPY手数料 任意` という表記も使わない
- P口座では、`Saxo実現損益 JPY` を主入力にし、`取引費用 JPY` は照合用にする。必要に応じて `手数料 JPY`、`為替変換手数料 JPY`、`取引所手数料 JPY`、`手数料・消費税額 JPY` を詳細内訳として入力できるようにする
- P口座の決済実績結果欄では、`Saxo実現損益 JPY` がある場合、計算内訳をJPY実績ベースで表示し、USD式を前面に出さない
- N口座 / PROD_N_USD_SETTLEMENT の決済実績では、USD約定価格、数量、USD手数料、USD実現損益を主入力にし、JPYは参考換算に限定する
- 注文ID、ポジションID、取引番号は通常入力から外し、`詳細ID・メモ` の折りたたみ領域へ移動する
- `決済済み` だが決済実績未入力の建玉に警告を出す
- 税務区分別履歴集計で、決済済み建玉は `optionCloseExecutions` の実現損益を優先する
- 決済実績年率を、建玉日から実際の決済日までの日数で計算する
- オプション損益の税務区分表示
- 株式譲渡損益の区分表示
- N口座USD損益の参考JPY表示
- 出金・円転イベントの記録欄
- 税務区分別履歴集計の通貨別注意表示
- 当年成績サマリーを追加する
  - 初期表示はブラウザローカル日付の当年
  - 結果状態かつ確認済み実績だけを集計する
  - 建玉管理画面の途中には常設せず、ヘッダーの `成績` ボタンから独立した成績画面として開く
  - 成績画面の上部に当年合計、オプション損益、株式譲渡損益、未確認件数を出す
  - 成績画面の詳細領域に月別実現損益チャート、年初来累計、税区分別内訳、銘柄別サマリーを出す
  - 未確認リストから該当入力カードへ遷移できるようにする
- JSONバックアップ/復元に決済実績を含める
- 旧JSONで `optionCloseExecutions` がない場合も壊れないようにする

完了条件:

- `注文前` から `建玉中` に変えた時、建玉約定確認カードへ自動で移動する
- 状態変更UIが画面上で分かりやすく、建玉状態をどこで変えるか一目で分かる
- 状態変更後に、次に何を入力するかのガイド通知が出る
- ダッシュボードに `次にやること` が表示され、クリックで該当カードへ移動できる
- 該当カードが折りたたまれている場合は自動で開く
- 該当カードと未入力欄が一時的にハイライトされる
- 注文前の予定値を初期値にして、約定後の正しい価格・数量・手数料・P口座JPY実績を確認できる
- P口座 / DEMOの建玉約定確認では、初期表示の入力項目が最小限で、`記帳額 JPY` を入れれば建玉時の現金反映額を記録できる
- P口座 / DEMOの建玉約定確認では、`手数料控除後受取額 JPY` という表記が出ず、`記帳額 JPY` または `現金反映額 JPY` と表示される
- P口座 / DEMOの建玉約定確認では、`プレミアム JPY - abs(取引費用 JPY)` を記帳額として計算しない
- P口座 / DEMOの建玉約定確認では、`取引手数料 USD 2.25` が通常入力として残っていない
- P口座 / DEMOの建玉約定確認では、為替レートと詳細内訳が補助欄に整理されている
- 約定情報が未確認の建玉には `約定情報未確認` が表示される
- 注文ID、ポジションID、約定時刻、決済時刻は通常入力に出ず、必要ならメモ欄に任意で残せる
- 反対売買判断は「判断用」、決済実績は「実際の約定結果」として分かれている
- Saxo注文履歴詳細の `買い決済 1プット @ 0.17` のような実績を入力できる
- Saxo注文履歴詳細のタイトル価格ではなく、明細行の `約定` 価格を実績価格として入力することが分かる
- P口座では、取引履歴に表示される `実現損益 (JPY)` をそのまま入力でき、税務・履歴集計にもその値が優先される
- P口座の通常入力欄は、Saxo取引履歴の `取引日`、`価格`、`実現損益 (JPY)` を中心に並ぶ。`記帳額 (JPY)` と `取引費用 (JPY)` は照合用詳細に畳む
- P口座の照合用詳細を開いても、費用内訳入力が一度に大量表示されない。費用内訳は二段目またはメモ欄で任意記録できる
- P口座の緑色の計算内訳は `Saxo実績ベース / 実現損益 xx JPYを採用` のようにJPYで表示される
- P口座で `Saxo実現損益 JPY` が入力されているのに、`プレミアム $... - 買戻し $... - 手数料 $...` のUSD式が表示されない
- P口座の決済実績カードに `決済時手数料 USD` が主入力として出ない
- P口座の決済実績カードに `JPY手数料 任意` という誤解を招く表記が出ない
- P口座では `Saxo実現損益 JPY` が入っていれば集計でき、詳細内訳は任意補助として扱われる
- 口座カードの `未反映の現金増減` は、現金残高へ手動反映するための任意導線として表示される。税務・履歴・年率計算の必須入力エラーとして扱わない
- P口座の現金反映で `記帳額 JPY` が未入力の場合、重複した警告文を出さず、`記帳額JPYを入力` ボタンを表示する
- `記帳額JPYを入力` を押すと、対象の建玉入力を開き、該当する `7. 決済実績` の `照合用の詳細` を開いたうえで `記帳額 JPY` 入力欄へ移動する
- N口座の現金反映情報不足では `記帳額JPY` というP口座用文言を出さず、`決済情報を入力` と表示する
- `現金残高に反映` ボタンは、反映額が計算できる場合だけ表示または有効化する
- N口座ではUSDベースの入力が主で、JPYは参考換算であることが分かる
- 注文ID、ポジションIDは通常入力欄ではなく、必要時だけ開く詳細欄にある
- `この価格で決済実績に反映` ボタンで、反対売買判断の価格を実績入力欄へコピーできる
- 反対売買判断からコピーした決済実績は下書き扱いであり、それだけで `決済済みに変更` は表示されない
- 決済実績カードで `決済実績を確認済みにする` を押した後だけ、`決済済みに変更` が表示される
- `建玉中に変更しました。次にSaxoの取引履歴を見ながら約定情報を確認してください。` の一時案内は、建玉開始確認後に残り続けない
- ローカル版、一般公開版、グレードアップ版で、同じJSONデータを入れた場合のダッシュボード `次にやること` が一致する
- 建玉中で確認済みの決済実績がある場合、`決済済みに変更` ボタンを表示し、押下時に既存の状態変更処理で `決済済み` へ遷移する
- 建玉中で満期終了記録が入力された場合、`満期終了に変更` ボタンを表示し、押下時に既存の状態変更処理で `満期終了` へ遷移する
- `決済済み` / `権利行使済み` / `満期終了` に変更された建玉は、ダッシュボードの現在管理中から外れ、履歴側に表示される
- 状態変更後は、履歴に移動したこと、または次に必要な入力先を短い通知で表示する
- `決済済み` にした建玉で決済実績が未入力の場合、警告が出る
- 決済済み建玉のオプション実現損益は、P口座では `Saxo実現損益 JPY` を最優先し、N口座またはSaxo実績JPYがない場合だけ、建て時プレミアム、買戻し約定価格、手数料から計算される
- 決済実績年率は、満期日ではなく実際の決済日までの日数で計算される
- 新規建玉、建玉約定確認、決済実績、株式取得、株式譲渡、移管、資金振替などの新規イベント日付は、ブラウザのローカル日付で本日を初期表示する
- オプションの満期日は契約条件として扱い、本日で自動上書きしない
- 既存保存データやJSON復元データの日付は、アプリ起動時やカード開閉時に本日へ上書きされない
- 日本時間の朝や深夜でも、UTCずれで前日・翌日が初期表示されない
- 建玉管理画面には当年成績カードが常設されず、建玉管理の視線を邪魔しない
- ヘッダーの `成績` ボタンから、当年の実現損益合計が一目で分かる成績画面へ移動できる
- 成績画面では、オプション損益と株式譲渡損益が別々に表示される
- 成績画面では、月別実現損益と年初来累計のチャートが見られる
- 未確認の決済実績、満期終了記録、株式取得/譲渡記録は当年成績に混ぜず、未確認件数として表示される
- N口座USD損益はUSD主表示、JPYは参考換算として表示され、P口座JPY損益と確定JPY合計として混同されない
- オプション損益と株式譲渡損益が自動相殺されない
- N口座のJPY参考換算が確定税務値として表示されない

### Phase 7: Saxo read-only / 候補アプリ連携

目的: 後続の自動取得版へ進む。

作業:

- ローカルAPI
- Saxo OAuth read-only
- 口座、建玉、残高、未約定注文取得
- オプションチェーン取得
- TradingView候補から建玉案作成

完了条件:

- 発注機能なしでSaxo情報を取得できる
- 手入力版のロジックに自然に接続できる

### Phase 7.5: 株価・為替外部取得の標準化

目的: ローカル版、グレードアップ版、一般公開版のいずれでも、株価・為替更新を標準機能として使えるようにし、ヘッダーの `外部取得ON/OFF` トグルを廃止する。

作業:

- ヘッダーから `外部取得ON/OFF` ボタンを削除
- 旧 `externalFetchEnabled` などのOFF保存値を無視またはON相当へ移行
- 株価取得ボタンと為替取得ボタンを常に有効化
- 送信される情報・送信されない情報の説明を `使い方` または `データ` 画面に移動
- 公開版で使える価格取得経路の実装または既存 `/api/quote` `/api/fx` の公開環境対応
- エラー時は手入力へ戻せる表示

完了条件:

- ヘッダーに `外部取得ON/OFF` ボタンが表示されない
- 保存済みデータにOFFフラグがあっても、株価・為替ボタンが使える
- 株価・為替ボタンがOFF状態で無効化されない
- 送信される情報が `使い方` または `データ` 画面で確認できる
- 建玉数量、口座残高、証拠金、JSONデータ、localStorage内容を外部送信しない
- 取得失敗時は既存値を壊さず、手入力継続できる
- 公開版でビルド・表示確認が通る

## 13. 最適な実装順

最初にやるべき順番は次。

1. Phase 0: 保全
2. Phase 1: P/N口座基盤
3. Phase 2: P/N別計算
4. Phase 3: P→N株式移管
5. Phase 4: N口座ホイール可視化
6. Phase 5: 警告と文言
7. Phase 6: 税務・履歴集計
8. Phase 7: read-only連携

理由:

- 口座と通貨の土台を直さないままホイールを作ると、後で全面修正になる
- N口座ホイールはUSD台帳が前提なので、P/N別計算が先
- P→N株式移管はホイール合流の入口なので、ホイール可視化より先
- read-only連携は最後でよい。手入力で正しい設計を固めてから取得自動化する

## 14. 初回実装対象

初回実装は、ユーザーが利用中のローカル版を対象にする。

```text
http://127.0.0.1:5173/
```

公開用GitHub Pages版、グレードアップ開発中版への反映は、ローカル版で確認した後に行う。

## 15. 完了判定

この改訂の完成条件:

- P/N口座を選んで建玉登録できる
- DEMO口座が `DEMO / JPYベース` として表示され、N口座欄が出ない
- P/N別に現金残高と証拠金使用率を管理できる
- N口座建玉はUSD主表示になる
- N口座のJPYは参考換算として表示される
- P口座で取得した株式をN口座へ移管できる
- N口座ホイールが銘柄別に表示される
- ホイールの現在フェーズが色付きで一目で分かる
- ホイールの累積損益がUSDで分かる
- オプション損益と株式譲渡損益が別区分で表示される
- JSONバックアップで復元できる

## 16. 実装時の禁止事項

- P/N口座の余力を合算しない
- N口座の参考JPYを確定損益として扱わない
- P口座取引に0.25%を一律上乗せしない
- 株式移管を売却損益として扱わない
- ホイール累積損益に未売却株の含み損益を実現損益として混ぜない
- Saxoへの発注機能を作らない
- OAuthトークンをブラウザlocalStorageに保存しない

## 17. 2026-06-08 手入力版MVP監査とAPI移行方針

詳細は次の設計メモに分離する。

```text
docs/manual-mvp-audit-and-api-transition-2026-06-08.md
```

結論:

- 手入力版は、建玉管理アプリとして予定していた主要機能をほぼ満たしている
- 今後は手入力版へ大きな新機能を追加し続けるより、手入力版をMVPとして固定し、Saxo API read-onlyによる数値取得版へ設計の主軸を移す
- API版へ移る前に、ローカル版、一般公開版、グレードアップ開発版の差分を確認し、意図しない差分を解消する
- API版でも発注機能は作らない
- 最初のAPI実装単位は、建玉取得より先に `口座・残高取得` を推奨する

API版の優先取得対象:

1. OAuth / 接続状態
2. P/N口座識別
3. 口座残高、必要証拠金余力、証拠金使用率
4. 現在建玉
5. 未約定注文
6. 決済済み・取引履歴
7. オプションチェーンと現在プレミアム

手入力版MVP固定の完了条件:

- ローカル版でテストが通る
- 一般公開版にも必要機能が展開されている
- グレードアップ開発版にも必要機能が展開されている
- 3版の仕様差分が意図した差分だけになっている
- マニュアルが現在のUIと一致している
- JSONバックアップ / 復元で主要データが壊れない
- 手入力版ではSaxo API連携が未実装であることが明確

先行して実装担当が行う工程:

1. 手入力版MVP固定
2. 3版差分確認と同期

この2工程の詳細は、`docs/manual-mvp-audit-and-api-transition-2026-06-08.md` の `11. 先行実装工程: 手入力版MVP固定と3版同期` に従う。

## 18. 2026-06-16 Saxo N口座現物株候補とP→N株式移管UX

### 18.1 目的

公開版でも、Saxo APIから取得したN口座の現物株ポジションを、通常のオプション建玉候補と混同しない。
P口座でプット売りが権利行使されて取得した株式が、N口座へ移管された場合は、`新規建玉` ではなく `P→N株式移管確認候補` として扱う。

### 18.2 想定ケース

P口座でプット売りが権利行使され、P口座でNVDA 100株を取得した。
その後、Saxo側でP口座からN口座への株式移管申請が通り、Saxo現在建玉APIではN口座にStock 100株が表示される。

実データ例:

- 口座: N口座
- AssetType: `Stock`
- 数量: 100
- 建て単価: 207.5 USD
- CorrelationTypes: `AutoAssignment`
- symbol / currency: 現在建玉APIでは未取得になる場合がある

この実体は、新しいオプション建玉ではない。
通常の株式買付でもない。
P口座で権利行使取得した株式がN口座へ移管された状態として扱う。

### 18.3 現行UXの問題

Saxo現在建玉の `Stock` ポジションを通常の建玉候補に混ぜると、次の誤導線が起きる。

- `建玉候補 / 要確認候補` として表示される
- `新規建玉として下書き作成`
- `3-A 建玉開始の約定確認` へ誘導される
- 銘柄未取得のため、ユーザーにティッカー入力を求める

これはP→N移管ケースでは不適切である。
3-Aはオプション建玉開始や通常建玉の約定確認の導線であり、P→N株式移管の確認導線ではない。

### 18.4 正しい分類

Saxo現在建玉APIで次の条件を満たすポジションは、`P→N株式移管確認候補` として扱う。

- `kind === "stock"`
- N口座に割当済みのAccountKey、または `accountAssignment === "N"`
- `quantity > 0`
- `averageOpenPrice` または `OpenPrice` が取得できる
- `CorrelationTypes` に `AutoAssignment` がある、または
- アプリ内に同銘柄・同株数・同取得単価のP口座権利行使済み `stockAcquisition` がある

この候補には、`建玉入力へ下書き反映` や `新規建玉として下書き作成` を主導線として出さない。
3-Aへも誘導しない。

### 18.5 照合ロジック

N口座Stock候補と、既存のP口座権利行使済み建玉を照合する。

照合条件:

- 既存建玉の `status === "assigned"`
- `stockAcquisition.enabled === true`
- `stockAcquisition.accountEnvironment === "PROD_P_JPY_SETTLEMENT"`
- `stockAcquisition.shares === Saxo株数`
- `stockAcquisition.priceUSD` とSaxo `averageOpenPrice` が一致、または許容差内
- 可能なら銘柄一致

Saxo現在建玉APIでsymbol未取得の場合は、同株数・同単価・直近の権利行使済み候補を提示し、ユーザー確認にする。

一致候補が1件だけの場合:

- `N口座に100株が確認されました`
- `P口座で権利行使取得した株式の移管候補です`
- `P→N株式移管を記録`
- `対応するP口座の権利行使済み建玉を開く`
- `今回は無視`

一致候補が複数ある場合:

- 候補一覧を表示し、ユーザーに選ばせる
- いきなり下書き作成しない

一致候補がない場合:

- `N口座に現物株があります。既存のP口座取得株と一致しません`
- `現物株として確認`
- `今回は無視`
- `新規買付として記録` は将来対応。現時点では主導線にしない。

### 18.6 画面導線

反映待ちサマリー:

- 旧: `建玉候補 新規0件 / 既存候補0件 / 要確認1件`
- 新: `P→N移管候補 1件`

候補表示:

- `Stock` かつN口座の場合、通常のオプション建玉候補から分ける
- 見出し: `N口座の現物株候補`
- 状態: `P→N移管確認候補`
- 説明:
  - `SaxoではN口座に100株があります。P口座で権利行使取得した株式の移管候補として確認します。`
  - `これは新規オプション建玉ではありません。3-Aには進みません。`

主ボタン:

- `P→N株式移管を記録`

補助ボタン:

- `対応するP口座の権利行使済み建玉を開く`
- `詳細を見る`
- `今回は無視`

### 18.7 移管記録の効果

`P→N株式移管を記録` を押した場合:

- `StockTransferEvent` を作成する
- `fromAccountCode = "P"`
- `toAccountCode = "N"`
- `shares = Saxo株数`
- `costBasisUSD = P口座取得単価またはSaxo averageOpenPrice`
- `sourceSimulationId = 対応するP口座権利行使済み建玉ID`
- N口座ホイールサイクルを `n_stock_holding` へ進める
- 移管は売却ではないため、株式譲渡損益・為替損益・オプション損益には加算しない

### 18.8 完了条件

- N口座Stock候補が、通常のオプション建玉候補と分けて表示される
- `Stock 100株` に `新規建玉として下書き作成` を主導線として出さない
- 3-Aへ誘導しない
- 既存のP口座権利行使済み `stockAcquisition` と照合する
- 一致候補が1件なら `P→N株式移管を記録` を主ボタンにする
- 移管記録作成後、N口座ホイールが `n_stock_holding` になる
- 移管記録は売却損益、為替損益、オプション損益へ混ぜない
- APIからsymbolが未取得でも、株数・取得単価・AutoAssignment・既存取得株から候補提示できる
- 公開版でビルドし、GitHub Pagesへpushして公開URLで確認する

### 18.9 移管記録後の完了導線

`P→N株式移管を記録` を押した後、メッセージだけで終わらせてはいけない。
ユーザーは「N口座ホイールで確認してください」と言われても、どこへ行けばよいか分からない。

記録成功後は、候補カードを次の状態へ変える。

- 主状態: `P→N株式移管を記録済み`
- `P→N株式移管を記録` ボタンは無効化、または `移管記録済み` 表示に変更する
- 同じSaxo Stock候補から二重に `StockTransferEvent` を作らない
- 主ボタンを `N口座ホイールを確認` に変更する
- 補助ボタンとして `JSONバックアップを保存` を出す
- `対応するP口座の権利行使済み建玉を開く` は補助に残す

`N口座ホイールを確認` を押した場合:

- Saxoパネル内ではなく、アプリ本体の `ホイール管理` セクションへスクロールする
- `ホイール管理` が閉じている場合は自動で開く
- 対象銘柄のホイールカードをハイライトする
- 表示期待値:
  - 銘柄: NVDA
  - 状態: `N口座ホイール管理`
  - 現在フェーズ: `N株式保有`
  - 現在株数: 100株
  - 平均取得単価: 207.5 USD
  - 移管履歴: `P口座からN口座へ100株を移管`

完了メッセージ文言:

```text
P→N株式移管を記録しました。
次は「N口座ホイールを確認」で、NVDA 100株がN株式保有になっていることを確認してください。
確認後、JSONバックアップを保存してください。
```

この状態では、ユーザーに再度Saxo候補を操作させない。
